# Truffle — Python dependencies
#
# Runtime: Python 3.11 (see .python-version). The engine is deliberately near-stdlib
# (HTTP is urllib, not requests; images via system tools, not Pillow), so this list is short.
#
# Install:
#   pip install -r requirements.txt
#   playwright install chromium      # only for the visual-evidence Tier-B browser re-render
#
# System tools (NOT pip — install separately; macOS-only today):
#   sips      built into macOS (image measure/resize)
#   magick    ImageMagick, for tile cropping  ->  brew install imagemagick
# Capture, query, and render work without these; only /visual-evidence + screenshot embeds need them.

PyYAML>=6.0        # frontmatter parsing — core (capture, query, render)
rapidfuzz>=3.9     # typo-tolerant company lookup suggestions; store.py falls back to stdlib difflib if absent
playwright>=1.54   # visual-evidence Tier-B browser re-render (scripts/shoot.py)
pytrends>=4.9      # Google Trends signal tool (tools/trends.py); pulls in pandas transitively
