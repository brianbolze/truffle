# Retro — clerky.com: productized-legal (fixed-fee + à-la-carte) offerings.md

Third non-Rx run, and a **third distinct shape**: not per-SKU (telehealth), not per-seat plan (notion), not marketplace catalog (airbnb) — a **productized-services price card**: two fixed-fee one-time packages ($427 Pay Per Use / $819 Lifetime) + à-la-carte components + state-fee pass-throughs. 21 rows, lint-clean ([`store/clerky-com/offerings.md`](../../../store/clerky-com/offerings.md)).

- **Bent the contract — held better than the siblings, strained only at leaf price.** `Kind` family/buyable **held honestly** — the components are genuinely purchasable (via package or pay-per-use), not the free/bundled fictions notion/airbnb forced `buyable` onto; only the free Attorney Account stretched it. `Visibility` **held without forcing** — because the two fixed-fee anchors give *every* component a price path, I never had to fake `on-request` (airbnb's coming-soon problem); `published` = bundled-free or the package itself, `partial` = "all-in depends on which package + an unshown à-la-carte number." `Slug` strained as usual (13 of 21 are `(no PDP — /startups/products#<line>)`) but **milder** — each points to a real attested *line anchor*, a shared-but-honest within-company key, not a buried tag id. `molecule · form · access` → dropped molecule (sanctioned), led with `form · access`. **The one real strain: `Price (verbatim)`** — see below.
- **Added nothing to the spine — right call again.** Folded package-inheritance into the Price cell + one earned Deep block (the pricing-model explainer). Resisted a `Tier`/`Package` column. But note the **n=3 convergence**: notion added `Category`, airbnb watched `Status`, and all three of us improvised the *same* missing thing — a way to say **"price lives at a parent bundle, not this row."** That's no longer a per-shape quirk; it's the spine's one structural blind spot for any bundled seller.
- **Where OFFERINGS.md was silent — the exact guess.** Same gap notion hit, independently: **no price token for a component whose price is inherited from a fixed-fee package.** Verbatim-or-`—` only; `—` means "family umbrella," but these leaves aren't umbrellas — they're priced, just *upward*. I guessed **`incl. in $427 / $819`** (notion guessed `incl. (Business $20)` — convergent invention = real gap). Second silent spot, new to my shape: **where does the price card live when it sits a tier *above* the roster grain?** The contract rows SKUs; Clerky's actual price card is 2 packages. I had to *decide* packages are buyable rows at the top that leaves inherit from — the doc gives no steer on a fixed-fee bundle that out-ranks the leaf grain.
- **One change — collapse three shape-notes into one bundled-pricing rule.** The siblings each asked for a shape-specific paragraph (notion: non-SKU/bundled; airbnb: Catalog/marketplace). Don't write a fourth ("productized-legal"). Instead add **one** subsection to OFFERINGS.md that blesses the shared pattern: (a) an **`incl. (<parent> $X)`** price token for bundle-inherited rows; (b) a **parent-priced bundle/package/plan row that leaves inherit from** is legitimate and *is* the price card when the verbatim anchors live there, not on the leaf; (c) **`Kind`/`Visibility` values are advisory** for non-storefronts. That single rule retires notion's `incl.`, airbnb's free/coming-soon, and my package-inheritance in one stroke — cheaper than per-vertical notes that'll keep accreting.
- **Verdict — fit, ~80%, the cleanest non-Rx run yet.** Because Clerky has two *real verbatim* fixed-fee anchors, the module captured the price card faithfully and the spine held everywhere except leaf-price-inheritance (the familiar bundled gap). **Brian's question — a real price-card that's neither SKUs nor seats? Yes, unambiguously:** a fixed-fee + à-la-carte + 3rd-party-pass-through card, fully rostered. The only friction is altitude — the spine assumes price-at-the-row; productized-services price-at-the-bundle. One token + one "bundles can be the price card" line closes it.
