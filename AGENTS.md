# Truffle — Codex entrypoint

Read [`CLAUDE.md`](CLAUDE.md) for project orientation.

When working on the engine surface (`_design/**`, `experiments/**`, `scripts/**`, `skills/**`, `tools/**`, `BACKLOG.md`), read [`.claude/rules/engine-dev.md`](.claude/rules/engine-dev.md) before proposing or making changes.

When touching Python, also read [`.claude/rules/python.md`](.claude/rules/python.md).

Do not assume `.claude/rules/` auto-loads in Codex; those files are shared rule sources that must be read explicitly.
