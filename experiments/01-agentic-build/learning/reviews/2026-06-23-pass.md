---
date: 2026-06-23
proposed: 1
left: 0
---

> **Bootstrap pass — illustrative, not the steady-state pattern.** This is the n=2 seed trail. A real pass leaves *most* observations unconsolidated (a short `left` list is itself the failure mode) and rarely graduates a singleton. Read it as a worked example of the *shape*, not the cadence.

**Pass summary.** Read 2 observations (corpus is brand-new): proposed 1 lesson (`L001`, about-Truffle) and graduated 1 Brian-correction to `brian.md`. Both were clearly actionable at n=2, so nothing was left unconsolidated — as the corpus grows, most observations will sit unconsolidated, named with why, not promoted.

**Proposed / advanced.**
- `L001` — cohort census counts category membership from a whole-file grep instead of frontmatter, so prose mentions inflate the count · from [2026-06-23-cohort-count-grep-inflation-7f3a](../observations/2026-06-23-cohort-count-grep-inflation-7f3a.md) → see lessons.md
  - test 1 (stated generally): a category-membership count must read the structured frontmatter tag, not grep the whole body · test 2 (replaces): the cohort census recipe's whole-file grep — corrects it, not additive
  - gate: one miss severe enough to justify a guardrail (a silent count-inflation defect) · subject: **about-Truffle** → Agentic Build's workflow (a `/query-companies` recipe fix)

**Routed to the Brian lane.**
- [2026-06-23-brian-cut-proposal-fields-9c1b](../observations/2026-06-23-brian-cut-proposal-fields-9c1b.md) (`kind: brian-correction`) → distilled to `brian.md#simplify-dont-just-add`. A single Brian correction graduates to the brian lane; it needn't recur.

**Deliberately left unconsolidated.** None — only two observations exist, both consumed.

**Anti-Merge attestation.** No observation was edited, merged, summarized, or deleted. Each was stamped `consumed-by:` (→ `L001` / `brian.md#simplify-dont-just-add`) and otherwise untouched; compression happened only by adding records that point at observations.
