---
date: 2026-06-24
run: changes/_completed/2026-06-22-honest-query-time-groupings
kind: friction
---

**Saw.** Running `/agent-build-review` in `frame` mode, the rubric reviewer-context names is "the `/frame` skill + the value/pillar/persona links." But the `/frame` skill (`~/.claude/skills/frame/SKILL.md`) is a generic problem-clarification helper — it tells you to "wallow" and produce a concise frame; it gives almost no criteria a reviewer can grade an *existing* frame against. The actual review traction came from reviewer-context's own emphases ("has it jumped to solution-space?", root-cause, non-goals, open questions) plus inferring testable-success and value-anchor checks myself. The `proposal` and `change` modes point at packet fields / a real checklist skill; `frame` mode is the one that points at a non-rubric.

**Not claiming.** Not claiming frame mode can't be reviewed — it can, and was. Not proposing a fix (a frame rubric, or moving the criteria into reviewer-context). One sighting, one frame, on a deliberately-completed test packet.
