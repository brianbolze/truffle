---
date: 2026-06-24
run: changes/2026-06-22-signal-delta-sec-edgar
kind: friction
---

**Saw.** The `agent-build-decide` SKILL.md task step 3 lists the decision vocabulary inline as "build it? / merge it? / revise / park / cut". But the packet I ran was already `implemented` and independently reviewed, so the real fork was `merge` vs `hold-after-review` — and `hold-after-review` is a real value in the Decision Rules (2026-06-21-proposal.md line 143) that the skill body never names. I had to go to the proposal's Decision Rules to find the term that actually fit the stage I was in; the skill's own inline list would have steered me to the wrong word.

**Not claiming.** Not claiming the skill should re-list the full vocab (it points to Decision Rules deliberately). One sighting, one packet — just noting the inline gloss and the canonical set disagreed at the exact stage that mattered.
