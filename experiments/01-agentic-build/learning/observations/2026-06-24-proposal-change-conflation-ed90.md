---
date: 2026-06-24
run: changes/2026-06-22-best-of-listicle-coverage
kind: friction
---

**Saw.** I was asked to review this packet in `proposal` mode, but `proposal.md` had `Status: implemented`, an Implementation Receipt, and a deterministic-gate log baked into the single file. The packet directory held only `proposal.md` — no separate change/diff artifact, and no persisted `proposal-review.md`, even though the body twice cites a prior "Proposal review" that found the evidence threshold met. So reviewing "the proposal" meant judging a plan that already shipped, with the earlier review's findings available only as a one-line summary inside the artifact being reviewed.

**Not claiming.** Not claiming the packet should be split into separate files, and not claiming the prior review was skipped — only that proposal and change collapsed into one file with no standing review trail, and the mode boundary (judge the plan vs. audit the patch) got muddy as a result. One sighting.
