---
date: 2026-06-24
run: agent-build packet 2026-06-24-neighbor-discovery (/discover-neighbors verb)
kind: risk-miss
---

**Saw.** Both independent reviews I spawned (proposal-mode and change-mode) passed the packet — "accept", then "accept (merge)" — for a verb whose core value was broken. The reviews were genuinely independent and genuinely thorough, but on the wrong axis: I briefed both reviewers, and the `reviewer-context` rubric stresses *shape* — is the spend cap concrete, is `write_scope` held, does the SKILL reference Recipe 9 rather than paraphrase it. Neither review was scoped to ask "does the feeder actually surface the right companies?" So the independence mechanism worked while the review *criteria* were value-blind, and the accumulated green checks made a broken change look more trustworthy, not less ("looks complete but isn't").

**Not claiming.** Not claiming every review must execute the artifact (that costs spend), nor that independent review is worthless — only that for a packet whose entire point is an output-quality claim, a review chain that never tests the output certifies a confidence it has not earned.
