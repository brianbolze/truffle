# Developer Review

Question: **What Truffle system behavior does this run pressure?**

Do not default to "capture more data." Ask whether the run exposed a reusable system need: capture, structure, query, freshness, automation, access, or guardrails.

Market reads may make judgments. When they do, ask whether the read clearly crossed from State or Signals into Judgment, and whether Truffle should support that boundary or leave the judgment downstream.

## Capability pressure

| Capability | What did the run expose? | Smallest useful response |
|---|---|---|
| **Capture** | None new — the credibility checklist already exists in `telehealth.md` prose. | No-op. |
| **Structure** | The proof-device cut is consistent prose across ~54 packs but lives below frontmatter. A `proof_devices:` frontmatter block would turn this read into a one-line query. Watch only — one sighting of "promote prose to frontmatter"; not worth schema churn until the second sighting. | Hold. See MRL-003 note below. |
| **Query / access** | Aggregating a cohort-pack credibility cut is a manual grep-and-classify pass — the same MRL-002 recipe gap. This is the Nth surface demonstrating the need for a cohort-pack read recipe; it belongs in the MRL-002 evidence log, not a new item. | Evidence log entry for MRL-002. |
| **Freshness / automation** | None. | No-op. |
| **Synthesis** | The run's design answer ("query-time-grouping-enough + already-captured") is sound and held cleanly. State/Judgment boundary was maintained throughout. The error bar disclaimer (±2–3) was included but the actual claim errors from the evidence verifier ran 3–9 out for four of five tallies — the prose-classification pass overconfidently collapsed a positive-vs-negated ambiguity in multiple device families. | Evidence log entry for MRL-008 (new flavor: prose-to-positive/negative polarity error in a Judgment-adjacent checklist layer). |
| **Guardrails** | The claim-not-truth convention was explicitly applied and held. The "not shown" ≠ "brand lacks it" framing was consistent. The evidence verifier's most material catch — four domains named as pharmacy-accreditation positives whose store packs explicitly say "not shown" — is a prose-classification error, not a boundary violation. The read correctly never asserted claim-truth. | No new guardrails item; folds into MRL-008. |

## Lenses

**Steward** — The store-only contract and State/Judgment boundary held cleanly throughout. The "not shown" = "not found on captured pages" framing was applied consistently and is the correct epistemic posture. The evidence verifier's four real catches (C2 LegitScript overcounted ~9 positives; C3 four named positives whose packs say "not shown"; C4 clinician split substantially off; C5 struthealth 180-day fabricated) are all prose-classification errors, not store mutations, truth-claims, or boundary violations. The struthealth 180-day citation is the sharpest failure — a named exemplar with zero support in the pack — and it lands in the same family as run 019's `weak`/`poor` polarity miscount: **the prose-classification pass on a Judgment-adjacent layer is the recurring failure mode, not the design question the read was asking.** The read's headline pattern (LegitScript is the floor, accreditation is the differentiator, clinicians track business model) survives all four corrections directionally; none of the catches invert the market finding.

**Dev Agent** — The friction the run actually surfaced is already named in MRL-002: aggregating a cohort-pack cut is a manual grep-and-classify pass with no helper and a real positive-vs-negated parsing risk. This run is the 8th read surface inside MRL-002's evidence log. The `proof_devices:` frontmatter-promotion idea is one sighting and relates to MRL-003's depth-backfill framing (promoting existing consistent prose to structured fields). It should not get its own item — runs 019 and 020 both had similar new-item proposals downgraded to Evidence Log entries. Same call here.

**Founder** — The decisive find — that the proof-device cut already exists in the cohort pack and the read is an aggregation, not a new extraction — is the valuable system signal. "Query-time-grouping-enough" is the right answer and is already the lab's dominant pattern (17th straight landing). No new primitive. The prose-classification error rate in this run is a quality signal, not a design signal: the prose cut is workable, just not exact. The `proof_devices:` frontmatter-promotion idea would reduce future classification toil modestly, but the honest "not shown" nuance a boolean would flatten is load-bearing — a `y/n/not-shown` enum does preserve it, but this is a depth-backfill question with one sighting, not a current priority.

## Recommendation

- **No-op / keep as observation:** `depth-backfill` pressure on the `proof_devices:` prose-to-frontmatter promotion — one sighting; do not create a new triage item. The run's own triage candidate (#3, "watch only") was self-correctly labeled. Hold for recurrence.
- **Watch for recurrence:** The State (device-presence) vs. Judgment (device-credibility) boundary is a new flavor worth adding to MRL-008 — not as a graduation driver, but as a named flavor the evidence log should carry. "Do not aggregate this layer's structured field; read the prose" (from MRL-008 run-019 visual entry) applies here in a different form: the structured sub-fields within the prose checklist (`y`/`n`/`not-shown`) are aggregatable, but the evidence verifier shows the positive-vs-negated classification pass on a Judgment-adjacent layer (credibility devices) carries a polarity-error rate that exceeds the claimed ±2–3 bar. Same family as run 019's polarity miscount.
- **Submit triage candidate:** Evidence Log entries for MRL-002 (8th cohort-pack read surface — credibility cut) and MRL-008 (new flavor: prose-classification polarity error on a Judgment-adjacent device-presence layer, consistent with run 019's visual-layer polarity miscount).

## Triage submissions

**MRL-002 Evidence Log entry (submit):**

> **2026-06-20 · Eighth read surface — cohort-pack credibility cut, with a prose-classification polarity-error quantification (run 021):** The proof-device read (LegitScript / clinicians / pharmacy-accreditation / commercial-trust flags) aggregated the `telehealth.md` "Health-merchant credibility" + "Payment & commitment" prose lines across 54 packs — the **eighth** State-read surface in the MRL-002 family (after price-posture/008, positioning/009, offer-structure/010, access/013, cross-cohort/015, competitive-set/017, visual-impression/019). Decisive finding: the cut already exists as an explicit `y`/`n`/`not-shown` checklist in cohort-pack prose; the read was aggregation, not extraction, confirming `query-time-grouping-enough`. The new, quantified sub-finding for the recipe: the Loop-2 evidence verifier found that 4 of 5 load-bearing device tallies fell **outside the read's own ±2–3 error bar** (errors ranged 4–9), with the largest miscount driven by positive-vs-negated polarity confusion on the LegitScript field (+~9 over-counted positive). The recipe guard established in run 010 ("quote, don't re-derive prose") extends here: **for a prose checklist where each cell has an explicit `y`/`n`/`not-shown` flag, re-classifying cells from prose context rather than reading the explicit flag is the failure mode** — the flag should be taken verbatim, then the tally run. Still recipe-level; no helper or stored frontmatter field wanted.

**MRL-008 Evidence Log entry (submit):**

> **2026-06-20 · Proof-device flavor — State/Judgment boundary on a credibility-checklist layer, and a polarity-error recurrence (run 021):** The trust/proof-device read surfaces a flavor that pairs with the run-019 visual-layer entry but on a different axis. (1) **State/Judgment boundary:** device-*presence* on a captured page (LegitScript seal visible in footer) is owned-page State and is correctly capturable; device-*credibility* or *differentiation* (whether the underlying pharmacy is actually in good standing, whether the named clinician holds an active license) is a Judgment the store must not harden. The read correctly applied this boundary throughout. (2) **Prose-classification polarity-error recurrence:** the Loop-2 evidence verifier found that 4 of 5 load-bearing tallies fell outside the read's ±2–3 bar, driven by the same positive-vs-negated misclassification pattern that miscounted run 019's visual polarity. Specifically: ~9 LegitScript-absent domains were read as positive; 4 of 7 pharmacy-accreditation named positives are explicitly "not shown" in their packs; the named-clinicians shown/not-shown split was inverted by ~9. A fabricated exemplar also appeared (struthealth 180-day money-back, no such language in the store pack). **The common failure mode: re-classifying cells from prose context rather than reading the explicit flag verbatim.** The fix is the same as the run-019 anti-footgun: when a layer has explicit value flags (`y`/`n`/`not-shown`; `strong`/`mixed`/`poor`), read the flag verbatim — do not re-derive from surrounding prose. Additive flavor on the polarity-error thread; does not move this item's graduation clock. Pairs with MRL-002 run-021 entry.
