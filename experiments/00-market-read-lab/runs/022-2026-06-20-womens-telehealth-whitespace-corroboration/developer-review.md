# Developer Review

Question: **What Truffle system behavior does this run pressure?**

## Capability pressure

| Capability | What did the run expose? | Smallest useful response |
|---|---|---|
| **Capture** | A named, concrete capture gap (dedicated women's menopause/HRT), not a "capture more in general" urge. | A human-gated `/research-company` worklist (tier-1/2) — propose, don't execute (MRL-009 shape). |
| **Structure** | `womens-HRT` is a 1-brand `anchor_category` cell hiding a 10+-operator market segment. | Watch `menopause/HRT` as a first-class `anchor_category` value — one sighting, do **not** graduate (new MRL-013). |
| **Query / access** | A **selection-bias** denominator is invisible to *every* store-only query by construction — no grep widens a corpus that was never captured. | Harden MRL-001's compound-caveat from "note" to a first-class QUERYING-whitespace guardrail: name both flavors and their **different tools**. |
| **Freshness / automation** | n/a (membership read, no temporal axis). | — |
| **Synthesis** | The bounded-live **external coverage-radar recipe** (SERP → ≥2 listicles → JSON-extract → cross-source intersection → store token-diff) generalized cleanly from run 012 (GLP-1) to run 022 (menopause). | Name it as a distinct *bounded-live* member of the MRL-002 read-recipe family — 2 sightings; do not build a helper. |
| **Guardrails** | Bounded-live contract held with zero drift; 3rd bounded-live run = the `review_after: 3` checkpoint. | Flag the checkpoint for a human glance at 011/012/022 together; no tightening indicated by this run. |

## Lenses

**Steward** — System stayed honest. State (store counts via grep) / bounded-live evidence
(2 dated, graded listicles + SERP) / Judgment ("asymmetry is coverage, not market") were
explicitly separated and labeled at the end of the Direct Answer. Absence language
disciplined; no size claim; affiliate confound surfaced. Verifier independently reproduced
all three load-bearing counts (store-5, intersection-9, 8-of-9-absent); the one fix (state
the name-variant normalization that turns the 7 exact-string matches into 9) is applied to
the receipt.

**Dev Agent** — The repeated toil here is *recognizable* (the coverage-radar recipe, now
2x) but not yet worth a helper — it's a few searches + 2 JSON scrapes + a grep. Name the
recipe shape; resist a script. The State/Judgment boundary tooling needs nothing new.

**Founder** — This compounds the warm/cited/cheap asset without ontology gravity: the
output is a worklist + Evidence Logs, not a new object, field, or score. It also de-risks a
bigger decision — whether to run a capture campaign to give the lab its "second cohort with
real depth" (the other under-tested direction runs 020/021 named) — by proving the gap is
real and naming exactly which brands close it.

## Recommendation

- **No-op / keep as observation:** —
- **Watch for recurrence:** `menopause/HRT` as a first-class `anchor_category` (new MRL-013,
  P3/Submitted) — revisit only if a tier-1 capture run confirms the density.
- **Submit triage candidate:** MRL-001 (selection-bias flavor **live-confirmed**; harden
  the two-flavors-two-tools rule — moves the *convention*, not graduation); MRL-002 (2nd
  sighting of the bounded-live coverage-radar recipe — name it); MRL-009 (concrete tiered
  capture worklist — propose-don't-write).

## Triage submissions

Appended as dated Evidence Logs to `triage.md`: MRL-001, MRL-002, MRL-009, and a new
**MRL-013** watch. State/Judgment boundary held; no unlabeled judgment found; no overclaim
of graduation. The capture-worklist follow-up is the highest-value next step but is **not**
autonomous-safe (Firecrawl spend → human approval).

**Do not graduate, spike, or implement system changes.**
