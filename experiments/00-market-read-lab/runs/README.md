# Market Read Lab Runs

These folders are run artifacts and learning history. They are **not** template
authority.

Use `../templates/` and the operator prompts for current conventions. Runs `000`-`002`
were produced before the autonomous contract settled, so read them only for evidence,
triage context, and pressure patterns. Do not copy their headers, stage behavior,
receipt rigor, or file shape into new runs.
