# Developer Review

Question: **What Truffle system behavior does this run pressure?**

> Reviewed via a 3-pass adversarial workflow (evidence verifier + consumer + developer, Sonnet). Verifier
> confirmed State-side membership independently (re-ran the 19-brand grep; spot-checked the four
> multi/none brands) and audited bounded-live discipline **PASS**. This developer pass judges whether the
> run's two triage attributions are justified or overreach.

## Capability pressure

| Capability | What the run exposed | Smallest useful response |
|---|---|---|
| **Capture** | Mochi (both listicles, absent) is a concrete capture candidate; LifeMD/Nurx/Wisp captured but GLP-1-unqueryable; altRx module-thin (already MRL-003). | Note Mochi; do not capture from inside the run. |
| **Structure** | The `anchor_category: GLP-1` grep **silently under-counts** GLP-1 offerers — multi/none and module-thin brands offer GLP-1 yet drop out of every cohort query. A structural-honesty issue, not just coverage. | Name the "anchored vs all-offerers" denominator cut in MRL-001; no schema change. |
| **Query / access** | The whole answer depends on which internal denominator is used; the anchor grep is the idiom but produces a biased count. | Documented reminder, not a helper. |
| **Freshness / automation** | Not pressured — listicle standing is ephemeral; the run declines to propose a stored leaderboard or monitor. | No action. |
| **Synthesis** | "Trust cross-source recurrence, not listicle order" is a small reusable convention for any future listicle-panel read; the prose award→brand extraction is the same prose-surface flavor as MRL-002 (run 010). | One sighting — watch, don't codify. |
| **Guardrails** | Affiliate-confound discipline was enforced by the agent, not by any system rule; held cleanly. | Candidate MRL-008 flavor append. |

## Lenses

**Steward — system still honest?** Yes. The "not found ≠ not true" guard held; the anchor-grep under-count
is the most important structural-honesty observation and correctly flows to MRL-001 rather than being
buried. State (store grep, primary) / Signals (listicle naming, secondary, dated) / Judgment (the "two
markets" interpretation, `[J]`-labeled) stayed cleanly separated. The verifier's two count errors were
presentation, not provenance — and were fixed, not hand-waved.

**Dev Agent — remove toil with a convention/recipe?** No new helper earned. The JSON-extraction cost lesson
(prefer markdown+parse at ~1 credit for named-set capture) is a useful operational note, not a convention —
a single-source pricing quirk. The prose award→brand recovery is MRL-002's known extract-from-prose grain
problem, not listicle-specific. Grep-verifiable store contract held.

**Founder — compounds the warm/cited/cheap asset while staying light?** Yes. ~7 credits, one session, one
receipt, a tiered capture-candidate list. No leaderboard object, no monitor, no schema. Mochi is the kind of
concrete output that compounds store value without ontology gravity; the "two markets" framing is a free
pattern a strategist uses immediately.

## Triage-attribution verdicts

- **MRL-001 (denominator reconciliation) — JUSTIFIED.** Two genuine additions: (1) first run to *operationalize*
  the SERP/listicle-as-fallback note — confirmed **radar, not denominator** (affiliate-confounded,
  head-stable/tail-divergent); (2) the **internal anchored-vs-all-offerers under-count** is real and
  *generalizable* — any multi-service cohort (GLP-1, hormones, longevity) will have brands that offer the
  category without being anchored to it, and the anchor grep silently drops them. This is the "internal set
  definition" side of MRL-001, complementing its current external-panel-fallback framing.
- **MRL-008 (source-rigor / confound) — JUSTIFIED but modest.** Listicle *inclusion* needs an
  affiliate-disclosure + cross-source-recurrence integrity sibling — a distinct flavor from run 011's
  Trustpilot score→body flavor (there: same source, different extraction depth; here: SEO/affiliate signal
  vs recurrence sub-signal). Additive Evidence Log entry; does **not** move MRL-008's graduation clock.
- **MRL-003 (Mochi capture candidate) — APPROPRIATELY SCOPED.** Correctly attached to the depth-backfill
  family as propose-don't-write, not a new item.
- **"Third-party default standing" Signal — correctly held.** One sighting; the run's own "one-sighting
  curiosity, not a primitive proposal" is the right treatment. Would only become a Signal-grain question if
  the same store-absent brands recurred across *many* surfaces (ads, Reddit, SERP).

## Recommendation

- **No-op / keep as observation:** the JSON-cost lesson; the prose-extraction friction; the "two markets" pattern.
- **Watch for recurrence:** the cross-source-recurrence convention (would become an MRL-002 recipe step if a
  future listicle-panel read repeats it).
- **Submit triage candidate:** MRL-001 Evidence Log append (radar-not-denominator + internal under-count);
  MRL-008 Evidence Log append (listicle affiliate-disclosure confound flavor). No new items; no graduation.

## Triage submissions

Append-only Evidence Log entries to **MRL-001** and **MRL-008** (below, in `triage.md`). No new queue items;
"no new primitive needed" is the honest outcome.
