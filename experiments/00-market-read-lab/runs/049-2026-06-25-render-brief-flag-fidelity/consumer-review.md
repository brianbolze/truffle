# Consumer Review

Question: **Where did Truffle create reader value, and where did it fall short?**

## Verdict

- **Valuable? Partly — and it lands on the builder/Steward, not the end reader.** This is a
  gap-probe whose payload is a roadmap finding about Truffle's own presentation surface, not a
  company answer a buyer/strategist consumes. Same builder-not-buyer frontier as 038/041/047/048
  CR1, here on the presentation layer.
- **Why:** the run's deliverable is "here is exactly where the shipped brief preserves vs. buries
  the trust flags," which the present-layer owner (and the engine Steward) can act on. A creative
  director reading a single brief gets no new company fact from this run.
- **Where Truffle added value:** it turned the abstract, recurring worry ("flag protection is
  relay-dependent") into a concrete, located, falsifiable claim about `scripts/present/brief.py` —
  and the evidence verifier *corrected the run's own overstatement* mid-flight (VR1), which is
  itself the value: the read now distinguishes structured-flag burial (real, structural) from
  default-path protection (real, but captor-prose-dependent).
- **Where it added little / fell short:** zero end-buyer value; n=1 relay (only the single-company
  HTML brief), n=6 profiles; the "5-second path" is an analytic DOM proxy, not an observed reader.
- **What the consumer can do now:** the Steward can decide whether tab-4 placement of the structured
  trust surface is correct-by-design or a salience bug; if a bug, the lightest fix (W1) is a
  `brief.py` ordering tweak (hero-grain flag eyebrow, or auto-open limits) — no schema/field change.
- **Safer than generic Claude + web search?** Yes for this question — it required reading the
  actual renderer source and diffing generated artifacts against dossiers; a web search can't see
  `_out/briefs/*.html` or `present/brief.py`.
- **Biggest limit:** single-relay, single-layout finding; says nothing about the comparison sheet,
  the index, or a delegated agent reading `profile.md` directly.
- **Human follow-up needed:** the present-layer owner's Judgment on whether the salience is
  intended; an out-of-band learning pass to decide if this consolidates with the 038/042/048 relay
  rows.

## Value diagnostics

| Signal | What to look for | Evidence / gap |
|---|---|---|
| **Useful** | Decision aid, not a summary. | Yes for the builder: a located, fixable salience finding. Not for the buyer. |
| **Judgment-ready** | Cited, rare ingredients. | The structural claims are cited to `brief.py` line ranges + generated HTML + `profile.md`; reproducible offline. |
| **Sourced & cited** | Traceable, uncertainty visible. | Receipt C1 grades evidence as derived/local; VR1 visibly corrects an overstatement. Strong. |
| **Deep enough** | Covers the intended set. | 6 profiles spanning flag types — adequate for the relay-structure claim; thin for any per-company generalization. |
| **Fresh enough** | Stale assumptions visible. | Renderer source read at HEAD; not freshness-sensitive. |
| **Kept / reusable** | Warm files for the next ask. | `_out/briefs/*.html` regenerable; receipt + read locate the exact code paths for a follow-up. |
| **Shortfall mapped** | Names what it can't support. | Explicit: single relay, DOM-proxy not reader study, salience-is-a-Judgment. |

## Job fit

| Job | Did the read help? | Missing / next step |
|---|---|---|
| **Make AI safe to delegate to** | Indirectly — it shows the *human* brief buries the flags a delegated read would also need to carry; the agent-reading-`profile.md` path is untested. | Audit a delegated-agent consumption path for the same flag salience. |
| **Five-second brief input** | This is the job under test, and the finding is that the 5-second surface carries the structured flags poorly (captor-prose-rescued only). | Decide if hero-grain flag surfacing is wanted. |

## Lens check

- **Strategist:** lands quickly *for a builder* — the one-line takeaway ("the brief preserves
  flags but buries the structured ones below the 5-second path; default protection rides on captor
  prose") is crisp. No buyer insight.
- **The Pantry / downstream system:** the read itself is a clean ingredient for the present-layer
  owner — cited code paths, a corrected claim, a held W1. Reusable without re-deriving.
- **First Contact:** would trust it — the verifier correction (VR1) is shown in the open, which
  *raises* trust rather than hiding the slip.

## Raw learning to preserve

CR row added to `run-notes.md` Observations (CR1). No lesson proposed; no system change.
