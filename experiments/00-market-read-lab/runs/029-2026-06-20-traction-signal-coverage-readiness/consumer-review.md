# Consumer Review

Question: **Where did Truffle create reader value, and where did it fall short?**

## Verdict

- **Valuable? Yes.** A gap-probe that does exactly its job: a decision-grade readiness map.
- **Why:** It gives the roadmap reader (and the frame's two traction consumers — per-company
  triage, cohort maps) a numbered, rung-by-rung verdict against the frame's *own* 5-rung ladder,
  with the load-bearing finding in the skim layer, and it never collapses *captured* / *comparable*
  / *rollup-ready* into one claim.
- **Where Truffle added value:** the 49→20→11 denominator chain (the grain discipline that is the
  whole point); the cited ladder table in the frame's own vocabulary; the named cheapest fix
  (sec_edgar delta branch, ~30 min); the concrete "what would change this" worklist.
- **Where it added little / fell short:** (1) the 20-company **axis distribution** lived only in the
  receipt, so a maps consumer had to chase R1 to see the substrate shape — *fixed in Loop 2* (added
  inline). (2) Rung-3 "Accumulate = Yes" skim-read implied durable accumulation while the captures
  are a one-week single campaign with no refresh cadence — *fixed in Loop 2* (cadence caveat added).
- **What the consumer can do now:** know precisely why a cohort traction-map isn't buildable yet
  (coverage 16% → comparability 9% → no same-axis cohort), and what unblocks it (a cohort capture
  campaign + the sec_edgar delta branch) — without re-deriving anything.
- **Safer than generic Claude + web search:** yes — every count is store-derived, cited, and
  qualified as a capture-campaign artifact, so the reader is protected from the two opposite wrong
  takeaways ("no traction layer" / "traction-ready"). A web search would have invented a market
  picture; this maps the *store's* picture honestly.
- **Biggest limit:** the substrate is a hormone/men's-health capture batch, so the read maps the
  campaign, not a market — correctly stated, but it bounds how far any consumer can push it today.
- **Human follow-up needed:** the spend-gated cohort capture campaign (not autonomous-safe).

## Value diagnostics

| Signal | What to look for | Evidence / gap |
|---|---|---|
| **Useful** | Decision aid, not a summary. | Strong — ladder verdict + ordered constraints + named fixes. |
| **Judgment-ready** | Cited ingredients to reason from. | Strong — C1–C8 all store-derived; the maps consumer can act on the rung verdicts. |
| **Sourced & cited** | Claims trace to dated captures/receipts. | Strong — one receipt, full claim map; capture clocks recorded. |
| **Deep enough** | Covers the intended set. | Yes — all 126 profiled / 49 signal-bearing / cohort overlays; no cherry-picking. |
| **Fresh enough** | Stale assumptions visible. | Improved in Loop 2 — the no-refresh-cadence caveat now sits on rung 3, not just buried. |
| **Kept / reusable** | Warm files for the next ask. | Yes — R1 inventory + the `traction-readiness` tag make a 2nd traction read cheap. |
| **Shortfall mapped** | Names where Truffle can't support. | Strong — this *is* the deliverable (coverage / comparability / cohort layer). |

## Job fit

| Job | Did the read help? | Missing / next step |
|---|---|---|
| **Build on top without re-capturing** | Yes — a downstream maps system gets a clean "here's the traction substrate and why it can't roll up yet." | The cohort capture campaign is the unblock. |
| **Trust the cache over time** | Partly — surfaced that nothing refreshes on a cadence (MRL-012); the traction layer is as live as its last manual capture. | Cadence design (MRL-012 sub-fix). |
| **Compare a whole field** | Mapped *why not yet* — no cohort has a same-axis comparable set. | Coverage, not architecture. |

## Lens check

- **Strategist:** Lands fast. Ladder table = the skim artifact; the one-liner names the constraint
  order. Minor: the *why* Wayback dominates (cheapest, ran-wide) is implied in Market Pattern #2 but
  could be one sentence sharper as a strategic lesson.
- **The Pantry / downstream system:** Gets clean ingredients — cited, store-derived, freshness
  visible, no unlabeled judgments. The axis-inventory pointer-chase to R1 was the one self-containment
  gap; now closed with an inline distribution line.
- **First Contact:** Would trust it. Absence language is consistently "not captured"; the ladder
  rungs are defined inline so a cold reader isn't lost. Evidence limits are thorough.

## Optional triage evidence

No new triage item from the consumer lens. Two value-miss observations (axis-distribution
inline; rung-3 cadence caveat) are preserved as **V2** in the run Discovery ledger and appended to
`discovery-ledger.md`; both were single-sighting synthesis-shape fixes applied in-place, not template
changes. **Do not graduate or implement system changes.**
