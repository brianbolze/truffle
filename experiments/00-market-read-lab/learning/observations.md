# Market Read Lab — observations

**Append-only stream of run learning signals.** One row per sighting; never merge, dedup,
rewrite, or reorder. Full contract: [`AGENTS.md`](AGENTS.md). Decisions live in
[`lessons.md`](lessons.md), not here.

Kinds: `friction` · `surprise` · `wish` · `gap` · `risk-miss` · `brian-correction`. Record the
symptom, not the fix. Neutral market findings stay in the run's `read.md`.

| Date | Run | ID | Kind | Saw | Not claiming | Evidence pointer | Tags |
|---|---|---|---|---|---|---|---|
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | O1 | surprise | First positive non-telehealth schema fit: all 5 marketplaces populate `offering_category: Marketplace / Platform` + `business_model: Marketplace / Commission` cleanly; both are designed-for closed-set values (TAXONOMIES:47 example cos = Airbnb, Uber). Contrast run-035 where investors got an *empty* business_model (subtractive gate). | That the schema fits all non-DTC shapes — only that the marketplace shape, unlike investor, is named positively + accurately by structured fields. | read.md Result(1), Market Pattern #1; TAXONOMIES.md:47 | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | O2 | gap | Marketplace economics (take rate, GMV, monetized side) have no structured field — all prose / `unverified_fields`. `target_market` gestures at two-sidedness but doesn't encode which side pays. | That a field is needed — n=5, heterogeneous, unit-incomparable numbers; prose likely enough. | read.md Result(2) table; etsy:67–72, doordash:65–67, upwork:59–60 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | S1 | surprise | Two-stacked-absence resolved toward the store's favor: the store is NOT blind to take rate — it holds verbatim fee schedules in prose for 3/5 (etsy 6.5%+, doordash 15–30%, upwork 3–10%). airbnb + uber simply don't disclose fee % on their marketing site (10-K scope). | That airbnb/uber don't charge a fee — only that it's not captured on-site. schema-can't ≠ firm-didn't-on-site. | read.md Result(3); airbnb profile.md:28/73, uber profile.md:71/106 | source-rigor, depth-backfill |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | G1 | gap | The SCHEMA price-visibility token `[published \| partial \| on-request]` has a grain mismatch on marketplaces: it tags the consumer offering price (airbnb Homes `[published]`), but a platform's own "price" as a product is its take rate on the other side, which the token never addresses. | That the token is broken — only that it answers "can a buyer get a price," not "what does the platform charge." A grain insight, not a defect to fix at n=5. | read.md Gap Map; SCHEMA.md:142; airbnb profile.md:62/65/68 | schema-edge-entity-type |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | G2 | gap | The filings / IR source family (10-K, investor relations) is the missing panel for take rate of marketplaces that don't disclose on-site (airbnb, uber). The SEC tool captures funding existence, not take-rate/GMV. Marketplace analogue of L003 (uncaptured decision-grade ingredient). | That the store *should* capture filings economics — only that it's the panel an off-site take-rate read would need. Spend/approval-gated. | read.md Source Gaps; tools/ (SEC = existence only) | source-panel, depth-backfill |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | G3 | gap | `business_model` (not `primary_industry`) is the only field that recovers the marketplace cohort — the 5 scatter across 5 industries. A naive industry draw is structurally blind to entity-shape cohorts. (Re-kinded friction→gap per dev review: the issue is structural blindness, not run difficulty.) | That this is a defect — it's the right key working; a reusable cohort-draw note that marketplace ≠ an industry. | read.md Companies Seen; primary_industry grep | denominator-reconciliation |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | W1 | wish | If anything graduates from O2, the lightest path is a documented prose/query convention for marketplace economics, NOT a per-marketplace field family — load-bearing reason is unit-incomparability (per-listing vs per-delivery-tier vs per-contract), not prose-by-default. Mirrors run-035 W1 anti-sprawl landing. | That it should graduate now — only the lightest path *if* a real cross-marketplace economics consumer + a 2nd homogeneous cohort appear. "No new primitive needed" stays live. | read.md What Would Change | query-time-grouping-enough |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | R1 | risk-miss | (Consumer review) The `[published]` token on airbnb Homes is a readability trap on a two-sided platform: a reader sees `[published]` and may infer Airbnb is a transparent pricer, when the platform's own host/guest service-fee split is explicitly absent. Token is locally correct; its label implies completeness it lacks. | That the token is broken or needs a fix at n=5 — only that the grain mismatch traps readers who don't read the token narrowly; sharper than G1. | consumer-review.md; read.md G1; airbnb profile.md:62/65/68; SCHEMA.md:142 | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/036-2026-06-24-marketplace-schema-fit | R2 | gap | (Developer review) Etsy's scale figure ($11.9B GMS) is a consolidated house-of-brands number (Etsy + Reverb + Depop) flagged in profile `unverified_fields`, but the read's economics table first presented it without the consolidated-scope caveat (now fixed). No structured entity-grain flag (single-brand vs house-of-brands) exists at frontmatter level — a reader can miss it without reading site_notes. | That the capture is wrong or a new field is needed — caveat is already in unverified_fields; only that read surfaces should propagate it and the store has no structured house-of-brands flag. | developer-review.md; etsy profile.md:25; read.md Result(2) table | source-rigor, depth-backfill |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | S1 | surprise | The single-valued `business_model` enum ("the primary model if several apply", TAXONOMIES:76) is structurally lossy on co-primary hardware+subscription revenue: all 4 connected hybrids (Oura/Whoop/Eight Sleep/Peloton) run two revenue legs (device + recurring) but the field names only one. The split survives only in prose / inline `STRAIN:` comments. Foils confirm the field is accurate when revenue is single-leg (Therabody/Hyperice/Nike pure one-time). | That the field is broken or a new field is needed — single-valued is a deliberate contract choice; this maps the cost at n=4, not a defect to fix. | read.md Result(1); TAXONOMIES.md:76; ouraring profile:48, whoop:40/42, eightsleep:44, onepeloton:43/64 | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | G1 | gap | No structured field encodes the **composite** revenue shape (which leg is required vs optional, who bundles whom). `offering_category` is a partial, inconsistent proxy — it carries the 2nd-offering type for Oura/Whoop/Peloton, but Eight Sleep's mandatory Autopilot sub is absent from it entirely (demoted to "companion" in a comment). | That a composite-revenue field should be built — n=4, single cohort, prose carries it fine for a human reader; "no new primitive needed" stays live. | read.md Gap Map; eightsleep profile:42/44 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | R1 | risk-miss | The primary-leg tag is not reproducible across structurally identical companies: Oura (device one-time + recurring app) → `Subscription`; Apple (device one-time + recurring services) → `Transactional / One-time`. A reader filtering `business_model: Subscription` for recurring-revenue businesses misses Apple; filtering `Transactional` for device-sellers misses all 4 wearables. The field answers neither "has a device" nor "has recurring revenue" for hybrids. | That either capture is wrong — both are defensible primary-leg reads; the risk is a reader trusting the single field to filter hybrids in/out. n=1 flip, not yet a proven defect. | read.md Result(2)/(3); ouraring profile:48, apple profile:40/65 | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | G2 | gap | Cohort-draw **inversion** of run-036 G3: there `business_model` recovered the marketplace cohort; here it *splits* a structurally coherent cohort (Subscription vs Transactional within connected hardware), and only looks like a key (`Hardware` ∧ `Subscription` recovers the 4 pure-plays, C1) because the four were tagged homogeneously — a coverage artifact, not a robust key. `primary_industry` scatters across 3 values for similar brands. | That `business_model` is a bad key in general — it's a per-cohort property; the note is that entity-shape cohorts need a query-time judgment, not a single industry/model draw. | read.md Companies Seen; C1 grep (19 Hardware profiles); run-036 G3 | denominator-reconciliation, schema-edge-entity-type |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | S2 | surprise | Second entity-type test for L006 (after marketplaces): the SCHEMA price-visibility token does **not** mislead on hardware hybrids — it reads correctly per-offering (device `[published]`, membership `[partial]`/`[published]`) because the buyer can obtain every price. Evidence that L006's trap is specific to two-sided/intermediary shapes, not non-DTC broadly. | That this graduates or alters L006 — decision boundary; an out-of-band observation that *sharpens L006's scope*, nothing more. | read.md What Would Change; lessons.md L006; whoop profile:65-67, oura profile:71-73 | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | W1 | wish | If anything ever graduates from S1/G1, the lightest path is making `business_model` a **ranked multi-select** (like `offering_category`) so a hybrid carries `[Subscription, Transactional]` — NOT a new per-company composite-revenue field. Reuses an existing multi-select pattern and resolves both reader-filters in R1 with zero new field family. | That it should graduate now — only the lightest path *if* a 2nd homogeneous hybrid cohort + a real consumer who needs to *filter* (not just read) by composite model appear. Mirrors run-036 W1 anti-sprawl landing. | read.md What Would Change; TAXONOMIES.md:110 (offering_category multi-select precedent) | query-time-grouping-enough |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | CR1 | gap | (Consumer review) The decision-relevant *blend* per brand (which leg is required vs optional, who bundles whom) is not in a scannable form — it lives in prose + `STRAIN:` comments, so a buyer comparing Eight Sleep (mandatory Autopilot) vs Oura (optional membership) must read paragraphs to extract the single most-decision-relevant fact. | That a new field is needed — an output/surfacing observation; the data is captured, just not in a scannable column. Inherent to the store-only surface; named, not hidden. | consumer-review.md; read.md Result(1)/Gap Map | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | DR1 | risk-miss | (Developer review) R1's Oura-vs-Apple flip is also a **future-capture** reproducibility risk, not just a reader-filter risk: if a new wearable is captured and tagged Transactional (device-dominant judgment), it silently exits the `Hardware` ∧ `Subscription` cohort that currently recovers the 4 pure-plays (C1). The run named this only as a hardening condition, not as a sighting. | That it is a proven defect — n=1 flip; only that the cohort-recovery query is fragile to inconsistent future captor judgment. | developer-review.md; read.md What Would Change ¶2; run-notes R1 | schema-edge-entity-type, denominator-reconciliation |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | DR2 | gap | (Developer review) The schema's own fail-loud channel (`STRAIN:` inline comments) is an **unreliable second channel** for the hybrid split: Oura/Whoop/Eight Sleep carry a STRAIN marker but Peloton carries none (split lives only in body prose). A reader can't rely on STRAIN to find every hybrid. | That STRAIN should be mandated or a fix applied — only that it is applied inconsistently, so it's not a dependable hybrid-detection surface. | developer-review.md; onepeloton profile:43/64 (no STRAIN); ouraring profile:48 | source-rigor, schema-edge-entity-type |
| 2026-06-24 | runs/037-2026-06-24-wearable-hybrid-revenue-schema-fit | DR3 | surprise | (Developer review) Sharper scope on S2/L006: the price-visibility trap fires not merely when an entity is "two-sided" but specifically when its **primary monetization runs through an intermediary leg that has no consumer-facing price**. Apple is partially intermediary (App Store/services) yet its token reads fine, because its consumer-facing device+service prices ARE published. | That this rewrites L006 — decision boundary; a tighter scope candidate for an out-of-band pass to weigh, not a run-side edit to the lesson. | developer-review.md; lessons.md L006; apple profile:38/40/59-60 | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | G1 | gap | First explicit test of the "make AI safe to delegate to" value job. **State-level geographic availability** is the systematically invention-forcing ingredient type for a delegated GLP-1 buyer brief: the "all-50/broadly available" binary is captured for ~3/8, but "can I get this in MY state" is unanswerable for ~6/8 (henrymeds doesn't enumerate; joinfound's ~40-state list is behind a gated picker; remedymeds/ivimhealth carry no state line). It is **intake-gated** — not on the marketing site — so even a refresh wouldn't recover it without entering the funnel. hellowisp is the exception (per-state license page). | That the store should capture state availability or that a field is missing — a source-scope (intake-flow) boundary the store flags honestly; per L004/L005 "not captured" ≠ "not available." | read.md Result(4); henrymeds unverified_fields:33; joinfound:35-36; remedymeds:27/127; hellowisp:93/137 | coverage-caveat, source-panel |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | R1 | risk-miss | Trust/proof + scale claims are captured *with* honest self-reported flags (henrymeds 4.4 self-reported; remedymeds 4.7 badge-image + "250,000+ members" internally inconsistent across its own pages; ivim "470K+"), but the flag lives in **prose**. A delegated agent that surfaces the captured claim without carrying its self-reported/unverified label launders marketing into apparent fact — protection is read-discipline-dependent, not structural. Live risk against the engine's #1 value job; delegation-relay grain of L002. | That the capture is wrong or a flag field is needed — the store flags correctly; only that the flag is prose-grade and depends on the downstream agent preserving it. | read.md Result(3); remedymeds:106/108/139; ivim:118; henrymeds:104 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | S1 | surprise | Grounding for a delegated brief is **ingredient-type-shaped, not brand-shaped**: the *same* brand grounds strongly on offer structure + advertised price and weakly on state availability + independent proof. The frontier cuts across the four types uniformly, not across "well-captured vs thin" brands — so "is this company agent-ready?" is the wrong question; "is this ingredient *type* agent-ready?" is right. | That grounding never varies by brand — capture richness still differs (hellowisp/hims deeper than remedymeds); only that the dominant axis of the frontier is ingredient type, not brand. | read.md Result(1)-(4); panel-wide pattern | query-time-grouping-enough, coverage-caveat |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | G2 | gap | The two grounding shortfalls (state availability; independent verification of price/scale/proof) share **one root**: the decision-grade fact lives **off the marketing site** — behind intake (price, state lists) or in filings/IR (audited scale for public cos hims/lifemd). Third sighting of the "decision-grade lives off the captured surface" boundary after run-036 G2 (marketplace take rate off-site) and run-037 Source Gaps (hybrid economics off-site), now hit by the grounding-for-delegation lens. | That the store should capture intake/filings — only that these are the source families a fully-grounded delegated brief would need; spend/approval-gated. | read.md Source Gaps; run-036 G2; run-037 Source Gaps | source-panel, depth-backfill |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | W1 | wish | If anything ever graduates from R1, the lightest path is a **read/relay convention** — "carry the self-reported flag and the intake-gated price caveat into any delegated output" — NOT a new provenance field or capture mandate. Load-bearing reason: the store already holds the flag; the failure is at relay, so the fix is a synthesis/output discipline, mirroring L004's "reconciliation travels with the read." | That it should graduate now — only the lightest path *if* a real delegated consumer + a 2nd cohort showing the same relay risk appear. "No new primitive needed" stays live. | read.md What Would Change; lessons.md L002/L004 | query-time-grouping-enough |
| 2026-06-24 | runs/038-2026-06-24-agent-delegation-grounding-calibration | CR1 | surprise | (Consumer review) Value-frontier mismatch: the read's value lands on the *builder/Pantry* consumer, not the end buyer, because the strongest-grounded ingredient type (offer structure) is NOT the buyer's deciding fact (all-in price + my-state availability), which are the two the store can't ground. A delegation read is therefore better pitched as a downstream grounding policy than as a buyer comparison. | That the read failed the buyer — it is a calibration, not a buyer's guide; only that the strongest grounding and the buyer's deciding facts are different ingredient types. | consumer-review.md; read.md Result(1)/(4) | coverage-caveat, query-time-grouping-enough |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | S1 | surprise | The store's relation support is **axis-asymmetric**: a first-class **vertical** relation primitive (`parent`/`owns`, rich on the SaaS slice — Qualtrics→Delighted, Twilio→SendGrid+Segment, AlphaSense→4 acquisitions, AWS←Amazon) but **zero horizontal** relation (competes-with / substitute / same-sub-market). Competitor neighborhood is structurally invisible while ownership is structurally first-class. | That a horizontal relation field should be built — only that the store's relation support is axis-asymmetric (vertical structured, horizontal absent). | read.md Result/Gap Map; parent/owns frontmatter across Tech slice | relation-pressure |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | G1 | gap | **First failure-side sighting of L005.** Structured fields can't resolve the SaaS sub-markets: `offering_category` is one `[Software / SaaS]` bucket for ~19 profiles, `target_market` mostly `[B2B]`, `business_model` mostly `Subscription` — none separates revenue-intel (Gong) from observability (Datadog) from survey (Typeform). Every prior L005 confirmation had the corpus *already carrying the cut*; here it does not, so grouping-by-enum yields one undifferentiated pile. | That `offering_category` is broken — it's a coarse leaf working as designed; the note is that query-time grouping fails when the enum has no sub-market leaf to group on. | read.md Result(1); frontmatter grep; lessons.md L005 | query-time-grouping-enough, relation-pressure |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | G2 | gap | Competitor neighborhood **is** carried — but only in **prose**, in ~5/23 bodies, at three different grains (Datadog clean list: New Relic/Dynatrace/Splunk/Grafana/Elastic; Gong per-product: Clari, Salesloft/Outreach; Dovetail comparison-set: Condens/Looppanel/Marvin; Listenlabs legacy+AI-native; Clari category posture only) — and the named rivals are **mostly off-store** (only Clari captured). Even where named, the edges dangle outside the slice. Unreliable-second-channel cousin of run-037 DR2 (STRAIN). | That the store should capture those rivals or add a competitor field — only that the edge data is prose-grade, uneven in grain, and points outward. Spend/approval-gated. | read.md Result(3)/Source Gaps; datadog:74, gong:59-60, dovetail:85, listenlabs:121 | depth-backfill, source-rigor, relation-pressure |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | G3 | gap | M&A-chain **consistency** gap in the vertical axis: the same Grammarly→Coda→Superhuman acquisition is recorded two ways — `coda.io` `parent: [grammarly.com]` vs `superhuman.com` `owns: [coda.io]` ("Grammarly rebranded"). "Who owns Coda" returns different owners from different profiles; both STRAIN-flagged, neither reconciled. `parent`/`owns` has no cross-profile reconciliation, so a chain can disagree with itself and both ends look clean. | That the captures are wrong — both are page-attested and honestly flagged; only that the vertical relation has no cross-profile reconciliation. | read.md Result; coda.io parent + superhuman.com owns | relation-pressure, source-rigor |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | S2 | surprise | `primary_industry: Technology` is a **leaky cohort key**: it returns apple, casio (hardware), eightsleep (smart hardware), upwork (`Marketplace / Platform`) alongside the ~19 genuine SaaS profiles. A naive industry-draw silently contaminates a SaaS neighborhood with 4 non-SaaS entities. Third sighting that industry ≠ entity-shape cohort key, after run-036 G3 (marketplaces scatter across industries) and run-037 G2 (hardware cohort needs judgment). | That industry should be re-tagged — the tags are individually defensible; only that an industry draw is not a SaaS-cohort key. | read.md Companies Seen; grep result | denominator-reconciliation |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | W1 | wish | If anything ever graduates from S1/G2, the lightest path is a **query-time recipe** ("read `description` + grep competitor body-lines for the sub-market") — NOT a competitor field or a sub-category `offering_category` leaf. Load-bearing reason: a competitor field would be mostly empty/dangling today (named edges off-store), failing engine-dev's "a field is a cut you can fill reliably" bar. Mirrors run-036/037 anti-sprawl W1 landings. | That it should graduate now — only the lightest path *if* a real cross-company neighborhood consumer appears AND the named rivals get captured. "No new primitive needed" stays live. | read.md What Would Change; .claude/rules/engine-dev.md | query-time-grouping-enough, relation-pressure |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | CR1 | surprise | (Consumer review) Value-frontier split: the read's strongest, most-cited output (competitor *edges* with captor provenance) is **not** the buyer's deciding fact, and its buyer-facing output (the 7-cluster sub-market map) is the run's own **Judgment, not store State**, so a downstream system can't consume it as an ingredient. The neighborhood is *diagnosable* but not *queryable* — a "map, not ingredient" frontier. Mirrors run-038 CR1 (read lands on builder/Pantry not buyer), here driven by State-vs-Judgment grain rather than ingredient-type. | That the read failed — it's a gap-probe that lands its design payload; only that diagnosis ≠ a queryable ingredient. | consumer-review.md; read.md Result(2) | query-time-grouping-enough, relation-pressure |
| 2026-06-24 | runs/039-2026-06-24-saas-competitor-neighborhood-resolution | DR1 | gap | (Developer review) `denominator-reconciliation` recurrence — `primary_industry` returning 4 non-SaaS entities is the **third** sighting that an industry draw is the wrong key for an entity-shape cohort (run-036 G3 marketplaces-scatter; run-037 G2 hardware-cohort-needs-judgment; now SaaS-draw-pulls-hardware/marketplace). Consistent enough to flag for a learning pass; still no build. | That a fix is due — only that the recurrence is now n=3 across distinct entity-shape cohorts. | developer-review.md; run-036 G3; run-037 G2; read.md Companies Seen | denominator-reconciliation |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | G1 | gap | Company **State has no append-only home**: `profile.md` is an overwriting current snapshot and the `signals/` append layer is documented as *external* source movement only (funding/reviews/visibility), with timeline machinery deferred. A brand's own pricing/offers/positioning change is structurally un-versioned, so the value job "what's new since last look" is unanswerable for company State today. | That an append-only State-history layer should be built — only that none exists; "no new primitive needed" stays live (no returning-reader consumer yet). | read.md Result(1); _design/2026-05-30-architecture.md:57/60/75 | freshness-monitoring, query-time-grouping-enough |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | G2 | gap | The only retained prior-State substrate, `captures/<date>/`, **co-mingles three capture purposes** (full capture / partial deepen-offerings / visual-evidence re-render) under one dated-folder convention with **no purpose or scope marker**. ~11/21 multi-capture domains' "second capture" is render-only or partial; only ~10/21 are diffable on 2+ dates, and even those rarely repeat the same page set (agelessrx 8→59 pages). | That the folder convention is broken — only that it isn't a clean diff substrate without a purpose/scope marker a reader can filter on. | read.md Result(2); receipts C1/C2; functionhealth 06-13 render variants / 06-16 tiles-only | coverage-caveat, denominator-reconciliation |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | S1 | surprise | In the **cleanest** diffable case (belmar, identical 4-page set 11 days apart) the homepage diff is 289 lines but ~all of it is **capture-method noise** — `www`/trailing-slash URL normalization plus an expanded nav mega-menu the earlier capture never rendered. Zero price/plan/offer lines changed. A store-retained raw-capture diff cannot separate real market change from scrape-depth/markup noise without per-line human judgment. | That belmar didn't change in reality — only that the retained diff can't distinguish real change from noise unaided. | read.md Result(3); receipt C3 | source-rigor, freshness-monitoring |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | S2 | surprise | The synthesis-lag hypothesis (profile `captured_at` earlier than the newest capture folder, true for ~all 21) **dissolved**: the newer folders are visual re-renders / partial deepens, not market re-captures. The capture clock is honest about the profile it describes; the divergence is the `captures/` dir mixing purposes. `captured_at` reads as per-profile freshness, not "newest thing the store knows." | That synthesis lag is impossible — only that this signal (clock vs newest folder) does not detect it; a real lag can't be ruled out without a per-folder purpose marker. | read.md C6; receipts C1/C2 | source-rigor, coverage-caveat |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | W1 | wish | Wanted a per-dated-folder marker of capture *kind* (full vs partial-deepen vs visual-render) and *scope*, so the diffable subset is selectable rather than hand-inspected each run. | That this should be built — only that its absence is what made the substrate un-diffable; lightest path (a folder marker) noted, held. | read.md Source Gaps; receipt C2 | tooling-ergonomics, coverage-caveat |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | CR1 | surprise | (Consumer review) Value-frontier lands on the **builder/Pantry** reader, not the buyer: the deliverable is "stop treating `captures/` as a change feed," which a downstream system consumes; a buyer asking "did this brand's price change" gets nothing. Third consecutive run with this frontier shape (run-038 CR1, run-039 CR1, now). | That the read failed the buyer — it's a gap-probe whose payload is a roadmap finding; only that the strongest value is not buyer-facing. | consumer-review.md; read.md Result | query-time-grouping-enough, coverage-caveat |
| 2026-06-24 | runs/041-2026-06-24-state-change-pulse-from-captures | DR1 | risk-miss | (Developer review) The load-bearing "capture-method noise dominates real change" claim (S1) rests on **n=1 same-page diff** (belmar). It is the cleanest available case and reproduces, but the store-wide generalization stands on a single brand-pair. | That the claim is wrong — belmar verifiably shows it; only that one same-page diff is a thin base for a store-wide generalization without a 2nd clean pair. | developer-review.md; receipt C3 | source-rigor, coverage-caveat |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | G1 | gap | The present-tense `description` field **launders vision into shipping** on pre-revenue deep-tech: electra "Builds the EL9," evoloh "Manufactures electrolyzer stacks," sorafuel "Produces sustainable aviation fuel" — yet electra has delivered zero, evoloh delivers 2026-27, sorafuel has no production (pilot still to be built). The single field most likely to be quoted in a cold-start over-claims maturity hardest. | That `description` is wrong or should encode stage — it accurately states positioning/intent; only that read as attainment it over-claims, and it's the maturity-blind field a shallow consumer leans on. | read.md Result(2), C3; electra/evoloh/sorafuel `description` lines | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | G2 | gap | No structured field encodes commercialization maturity. `business_model` is **blank** for cfs-energy + sorafuel (closed set has no "pre-revenue/no-model-yet" value — subtractive absence, cousin of run-035's empty-business_model-for-investors); the shared `offering_category` value `[Physical Products / Hardware]` is carried by both beta-team (public, shipping components) and electra (zero deliveries), encoding no maturity. The gradient compresses to flat across all structured fields. | That a maturity/stage field should be built — n=8, single cohort, prose carries it; a stage field would rot (captor judgment on a continuum, fastest-decaying, mostly-blank). "No new primitive needed" stays live. | read.md Result(2)/(3), C3; cfs+sora blank business_model | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | S1 | surprise | `unverified_fields` is the **unsung maturity-protector**: across all 8 it honestly flags exactly the maturity-relevant absences ("current production… planned pilot"; "pre-order pipeline is site-reported, not a price card"; "capital raised is self-reported"). The one near-structured surface guarding against over-claim — but a free-text list whose protection depends on the downstream reader carrying it (L002/L004/038-R1 relay shape, on the maturity axis). | That `unverified_fields` is sufficient or structured — it's prose-grade and relay-dependent; only that it, not any typed field, makes the read safe. | read.md Result(4), C2; `unverified_fields` across the 8 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | S2 | surprise | Counter to the recent "store falls short for the buyer" streak, the store here is a **genuine strength**: the 8 rank *cleanly* on maturity because bodies carry dated, specific anchors (IPO/10-Q, first-flight date, FAA cert-application date, NRC milestone, "operational in 2027", "pilot in 18–24 months"). The gap-probe found a positive — maturity is legible from State, just not queryable. | That maturity is always recoverable — depends on the captor writing a milestones block; only that for this cohort the prose carried it well. | read.md Result(1), C2; per-profile milestones | query-time-grouping-enough, source-rigor |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | G3 | gap | **Fourth sighting** of denominator-reconciliation: `primary_industry` scatters the cohort across Automotive (electra) / Manufacturing (verdego, beta) / Energy (other 5), and the energy draw pulls in **euclidpower** — a commercially operating renewable-energy services+SaaS firm, not pre-revenue deep-tech. An industry/category draw does not recover the entity-shape cohort "pre-revenue deep-tech." (After run-036 G3, run-037 G2, run-039 DR1.) | That industry tags are wrong — each is individually defensible; only that the recurrence (n=4 across distinct entity-shape cohorts) is now consistent. | read.md Companies Seen, C1; `primary_industry` grep; euclid profile | denominator-reconciliation |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | R1 | risk-miss | **Delegation over-claim risk against the engine's #1 value job.** A delegated agent quoting `description` + frontmatter would present sorafuel ("Produces SAF") and electra ("Builds the EL9") as shipping companies, when neither has commercial output. The protection (milestones + `unverified_fields` + "self-reported" labels) lives only in prose, so safety is read-discipline-dependent, not structural. Maturity-axis instance of run-038 R1. | That the capture is wrong or a flag field is needed — the store flags honestly; only that the guard is prose-grade and fails at relay if the consumer is shallow. | read.md Market Pattern, Result(2); G1; run-038 R1 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | W1 | wish | If anything ever graduates from G1/R1, the lightest path is a **read/relay convention** — "for pre-revenue/deep-tech, rank maturity from the milestones block + `unverified_fields`; treat present-tense `description` as positioning not attainment; carry the self-reported flag" — NOT a `stage`/`maturity` field. Load-bearing reason: a stage field is a rotting captor judgment, mostly-blank store-wide, failing engine-dev's fillable-cut bar. Mirrors run-036/037/039/040 anti-sprawl W1 landings. | That it should graduate now — only the lightest path *if* a 2nd pre-revenue cohort shows the same description-overclaim AND a consumer needs to *filter* by maturity. | read.md What Would Change; .claude/rules/engine-dev.md | query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | G4 | gap | The **filings / IR / trade-press** source family is the off-surface panel needed to independently date/confirm the self-reported milestones (10-Q for beta-team; press for the rest) the whole maturity read rests on. **Fourth sighting** of "decision-grade fact lives off the captured surface" after run-036 G2 (marketplace take rate), run-037 (hybrid economics), run-038 G2 (price/state/scale) — now on the maturity axis. | That the store should capture filings — only that it's the panel a *confirmed* (vs site-attested) maturity read would need; spend/approval-gated, not chased. | read.md Source Gaps; run-036 G2 / 037 / 038 G2 | source-panel, depth-backfill |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | VR1 | risk-miss | (Evidence verifier) The read called two `offering_category` lists "identical" for beta-team and electra; they only **overlap** — beta-team carries `[Physical Products / Hardware, Services / Consulting]`, electra only `[Physical Products / Hardware]`. A precision overreach the adversarial verifier caught; corrected in read.md / run-notes G2 / receipt. The structural point (shared value encodes no maturity) survives. | That the finding was wrong — only that the read overstated field-sameness; evidence that the 3-pass Loop 2 verifier catches precision slips a single-pass read ships. | verifier overreaches_found; beta-team profile:47 vs electra profile:43 | source-rigor |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | CR1 | gap | (Consumer review) The `description` over-claim propagates into the **body lead**, not just frontmatter: sorafuel's body opens "producing sustainable aviation fuel (SAF)" before the pilot-stage clarification. So the relay risk is wider than R1's "frontmatter-only" framing — any body-skimming read that stops before the milestones/credibility block still gets the over-claim. | That the body is badly structured — the full profile is honest and well-organized; only that the over-claim reaches deeper than "protection lives only in prose" implies. | consumer-review.md; sorafuel profile Overview + `description`; run-notes R1 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | CR2 | surprise | (Consumer review) `unverified_fields` is covering **two distinct failure modes**: missing-attainment (the pre-revenue cases) AND a *conflicting* on-site fact — euclid's entry flags "Support metrics vary by page (22 GW / 1,100+ / 31 states vs 26 GW / 950+ / 37 states)." Same guard mechanism, different risk patched. The run's S1 framing collapses both into "maturity-protector." | That this weakens S1 — `unverified_fields` still protects; only that it spans two failure modes, not one. | consumer-review.md; euclidpower profile `unverified_fields` | source-rigor, schema-edge-entity-type |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | CR3 | gap | (Consumer review) A **populated** `business_model` is equally maturity-blind: evoloh (ranked #2, order-book pre-delivery) carries `Transactional / One-time` — the same value a shipping electrolyzer seller would carry whether or not it has delivered. So blank-vs-populated does **not** signal pre-revenue status (sharpens G2: the blank is a subtractive absence, but the populated field is no better a maturity signal). | That G2 is wrong — blanks are still worth noting; only that the populated case carries the same blindness, so the gap is wider than "blank = pre-revenue marker." | consumer-review.md; evoloh profile `business_model` + 2027 delivery | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | DR1 | gap | (Developer review) The description over-claim is **5/7** pre-revenue profiles, not the 3 G1 named: verdegoaero "Develops and manufactures hybrid-electric aircraft powerplants" (no delivered commercial product) and cfs-energy "Builds commercial fusion energy systems… toward ARC" (zero power) also carry present-tense-implies-operation `description`s. Widens G1's scope rather than contradicting it. | That the 3 named are wrong choices — they're the most egregious; only that the over-claim scope is wider than the cited evidence set. | developer-review.md; verdegoaero profile:37; cfs-energy profile:38 | schema-edge-entity-type, source-rigor |
| 2026-06-24 | runs/042-2026-06-24-deep-tech-commercialization-maturity-read | DR2 | gap | (Developer review) beta-team is a **cohort boundary case**: it is unambiguously revenue-generating on non-flagship lines (UL-certified Charge Cubes are sold) while its flagship aircraft is pre-certification. The run places it at rank 1 ("early-commercial") honestly, but "pre-revenue deep-tech" as a cohort concept has **no clean structured boundary** even within its 7 members — the flagship-vs-company revenue line must be re-invented by each reader. | That the ranking is wrong or beta-team should be excluded — the treatment is defensible; only that the cohort concept itself resists a structured boundary. | developer-review.md; beta-team profile frontmatter + read.md rank-1 note | schema-edge-entity-type, denominator-reconciliation |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | S1 | surprise | The buyer's hardest composite fact — year-one TCO (device + required-vs-optional sub) — **is assemblable from captured State** for 4/5 connected sleep/recovery brands: device price, sub price, billing cadence, and mandatory-vs-optional status all present with capture clocks + point-in-time flags. A **positive** gap-probe result and the **buyer-value inverse** of run-037's schema-lens probe: the store carries the buyer's deciding fact, just not in one scannable column. Notably value lands on the **buyer**, countering the run-038/039/041 CR1 "lands on builder not buyer" streak. | That it's apples-to-apples — it's an honestly-caveated *range*, not a single sortable number (see S2). | read.md Result table; receipts R1 | query-time-grouping-enough |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | G1 | gap | The **required-vs-optional subscription** flag — the single most decision-relevant fact for year-one cost — lives only in prose / `STRAIN:` comments, never a structured field (whoop "you join, you don't buy"; eightsleep "required for the first 12 months"; oura "the only way to unlock… insights"; peloton "must hold an All-Access Membership"). A buyer reads paragraphs to learn if the recurring cost is mandatory. **Buyer-decision-relevance instance of run-037 G1/CR1** — same lossiness, now shown to matter (or not) for an actual purchase. | That a structured flag should be built — n=4, single cohort, prose carries it for a human read; "no new primitive needed" stays live. | read.md Gap Map(1); run-037 G1/CR1; whoop:63, eightsleep:68 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | S2 | surprise | The "year-one" unit is **structurally non-uniform** across the cohort, defeating a clean apples-to-apples sort even with all data present: whoop bundles the device *into* the sub (year-one = sub only, $0 device) while others separate them; cadence differs (whoop/eightsleep annual, oura/peloton monthly); rental paths (eightsleep $169/mo, peloton $124.99/mo) reshape the whole TCO. Normalizing needs per-brand judgment, not a field. **Cousin of run-023 GLP-1 price-incomparability, now on connected devices.** | That the numbers are wrong — each is captured correctly; only that they aren't unit-comparable across the cohort. | read.md Gap Map(2); run-023; whoop:35/63, eightsleep:75 | source-rigor, denominator-reconciliation |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | G2 | gap | **Apple Watch is the cohort member the store cannot answer** — Apple is captured at company/catalog grain (multi-product giant; only Mac entry prices quoted inline), with no Apple-Watch-SKU price. A single-product buyer question hits an **entity-grain mismatch**: a catalog-grain profile can't serve a SKU-grain buyer ask. **New flavor** of denominator-reconciliation — grain mismatch *within* a captured company, not cohort-draw contamination (distinct from the n=4 industry-draw pattern). | That the capture is wrong — catalog-grain is right for Apple; only that this grain can't serve a single-product TCO question. | read.md Gap Map(4), Result(C5); apple profile:33/40/59 | coverage-caveat, denominator-reconciliation |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | S3 | surprise | **Every device number is a point-in-time sale snapshot**, and the profiles flag it honestly — oura flash sale with a same-day price disagreement ($279 vs $399 Ceramic), eightsleep "4th July Sale", peloton "limited-time" refurb ($695, ends June 15). A year-one TCO inherits that volatility wholesale; the read is a dated snapshot, not a durable price. | That prices are unreliable in the store — captures are honest and clock-stamped; only that a TCO built on them is a snapshot. | read.md Gap Map(3); oura/eightsleep/peloton site_notes | source-rigor, freshness-monitoring |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | S4 | surprise | The one-time foils (therabody/hyperice/nike) confirm `business_model: Transactional / One-time` is **accurate when revenue is single-leg** — no sub, year-one = sticker price. Clean contrast proving the lock-in is a vendor *choice*, not a category inevitability. Mirrors run-037's pure-one-time foils (same three brands). | That the field is always right — only that it's accurate for the single-leg case; the hybrid lossiness (G1) is the real edge. | read.md foil table; therabody:51, hyperice:51, nike:49 | query-time-grouping-enough |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | W1 | wish | If anything ever graduates from G1, the lightest path is run-037 W1's landing — a **ranked multi-select `business_model`** or a structured required-vs-optional flag — and **only** if a real consumer needs to *filter/sort* brands by composite cost programmatically. This run is **positive evidence the human-read case does not need it**: prose carried the buyer decision today. | That it should graduate now — only the lightest path *if* a filtering (not reading) consumer appears. "No new primitive needed" stays live. | read.md What Would Change; run-037 W1 | query-time-grouping-enough |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | VR1 | surprise | (Evidence verifier) oura's membership is **value-gating, not strictly mandatory** — State says the ring "will still function… but the insights… much more limited," so oura is the **only** cohort member where a buyer can pay **$0 recurring** (device-only floor $244). The read's "required for value: yes" column is defensible but flattens a real $0-recurring path that distinguishes oura's lock-in from whoop/eightsleep/peloton (strictly mandatory). | That the read was wrong — the column captures it; only that the sharper buyer fact is "oura is the lone optional-recurring case." | ouraring profile:81; read.md Result table | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/043-2026-06-24-wearable-year-one-cost-of-ownership | CR1 | gap | (Consumer review) The year-one TCO is the run's own **Judgment** (an assembled, normalized range), **not** a store-queryable field — a downstream system can consume per-brand device + sub prices as ingredients but cannot consume "year-one TCO" as State. **Same "diagnosable but not queryable / map-not-ingredient" frontier as run-039 CR1, here on the price axis** rather than relations. | That the read failed — it lands its buyer value; only that the headline number is synthesis, not a queryable ingredient. | consumer-review.md; read.md Result | query-time-grouping-enough, denominator-reconciliation |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | S1 | surprise | **Third pricing-shape where unit-incommensurability, not missing data, is the ceiling.** All 6 metered dev-infra brands carry verbatim `/pricing` rates, but no two share a billing unit — Datadog $/host, Snowflake $/credit, Twilio $/message, PostHog $/event, Stripe %-of-volume, AWS per-service utility. "Which is cheaper" has no cohort answer without the buyer's own workload. After run-023 (GLP-1 per-month vs per-dose) and run-043 S2 (wearable TCO unit non-uniformity), now on metered B2B infra. | That the store under-captured — pricing is present, exact, and dated; only that the unit is each product's own consumption primitive, so cross-vendor magnitude doesn't compare. | read.md Result(1)/(2), C1/C2; datadog profile:54-64; snowflake:66-71; twilio:59-62; posthog:66-73; stripe:62-66 | source-rigor, denominator-reconciliation |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | S2 | surprise | `business_model: Usage-based / Consumption` is a **clean positive cohort key** — the grep returns exactly the metered businesses (+2 non-infra excluded by hand). Direct contrast to run-039 G1, where `[Software / SaaS]` flattened ~19 sub-markets into one undifferentiated pile. Here the field *recovers* the cohort the question is about. | That `business_model` is always a good key — it's coarse (single-valued, primary leg only) and pulls in non-infra metered firms; only that for *this* question it worked where the SaaS draw failed. | read.md Result(3), C3; run-039 G1; `grep business_model: Usage-based` | denominator-reconciliation, query-time-grouping-enough |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | G1 | gap | **Price-visibility token near-absent on the cohort.** Token (SCHEMA 2.3+) on only 1/6 — Stripe (23 `[published]`, 2 `[on-request]`). Datadog/Snowflake/Twilio/AWS are schema 2.2 (predate it); PostHog's `profile.md` is 2.6 yet carries **zero** tokens on its `What they offer` lines despite verbatim rates. A buyer using the token as a "can I get a price?" filter would be misled by omission (5/6 untokenized though all 6 publish prices). | That the token is broken or backfill is due — SCHEMA says absent = predates-convention, no backfill; only that the token isn't a dependable cross-cohort surface on captures this vintage, and PostHog's v2.6 profile hints it may not fit pure-metered sellers. | read.md Result(4), C4; stripe profile:62-66; schema_version 2.2 on 4/6; posthog profile 2.6 untokenized; SCHEMA.md:99 | coverage-caveat, depth-backfill |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | S3 | surprise | **Stripe is an L006 *non-trap* — a 2nd entity-type sighting on the safe side.** Stripe is an intermediary (payments) whose monetization (%-of-volume) **is** published to its paying customer (the merchant), so its `[published]` tokens read correctly. Matches run-037 DR3's sharpened scope: the L006 trap fires only when the intermediary leg has *no consumer-facing price* (marketplace take-rate split), not whenever an entity is an intermediary. Contributes to L006's graduation check (it requested a 2nd entity-type sighting beyond marketplaces). | That this graduates/alters L006 — decision boundary; only an out-of-band sighting that *confirms* run-037 DR3's scope on a new entity (a transparent intermediary). | read.md Result(5), C5; stripe profile:62; lessons.md L006; run-037 DR3 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | G2 | gap | Single-valued `business_model` **mis-sorts the hybrid**: Notion (`Subscription` primary + a usage-based Notion-credits layer riding on top, per its own inline comment) sits in the *foil* set, so a usage-based-revenue cohort drawn from the field misses it. Same single-valued lossiness as run-037 S1 (wearable hybrids), now on the metered-revenue axis. | That a field should be built — n small, single cohort; only that the cohort key recovers *pure-play* metered businesses, not every business with metered revenue. | read.md Result(3); notion profile business_model comment; run-037 S1 | denominator-reconciliation, query-time-grouping-enough |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | W1 | wish | If anything ever graduates from S1/G2, the lightest path is run-037 W1's landing — a **ranked multi-select `business_model`** (so Notion's hybrid is recoverable) — and **never** a normalized price-magnitude field: unit-incommensurability means a magnitude field would launder false precision, exactly what engine-dev's "evidence, not scores" forbids. This run is positive evidence the human-read case needs neither. | That it should graduate now — only the lightest path *if* a real consumer needs to *filter/sort* metered tools programmatically. "No new primitive needed" stays live. | read.md What Would Change; run-037 W1; .claude/rules/engine-dev.md | query-time-grouping-enough |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | CR1 | surprise | (Consumer review) **Buyer-primary value frontier — 2nd consecutive after run-043.** The run's value lands on the buyer (a dated, cited per-vendor rate table + a usable structural-ceiling framing a CTO can act on), not the builder/Pantry. The G1 token gap and S2 cohort-key contrast are builder-lane *byproduct*. Breaks the 038/039/041 CR1 "lands on builder not buyer" streak for a second value-read running. | That builder findings are absent — they're real but secondary; only that the *reason the run has value* is buyer-facing this time. | consumer-review.md; read.md Result(1)/(2); run-043 S1/CR1; run-039 CR1 | query-time-grouping-enough |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | DR1 | gap | (Developer review) **Precision correction to G1.** PostHog's `offerings.md` is on the *module*-schema track (v1.2), which predates the token convention independently — a routine module-backfill gap, NOT part of the "post-2.3 yet untokenized" tell. The real signal is the v2.6 *profile* being untokenized; merging both over-stated the "convention may not fit metered sellers" hypothesis. (Corrected in read.md.) | That the profile-level tell is wrong — the v2.6 profile untokenization stands; only that the offerings.md absence has a separate, mundane explanation the read conflated. | developer-review.md; posthog profile:3 (2.6) vs offerings:3 (1.2); SCHEMA.md:99 | source-rigor, depth-backfill |
| 2026-06-24 | runs/044-2026-06-24-usage-based-pricing-comparability | DR2 | surprise | (Developer review) **S2 ↔ run-039 G1 is one field, cohort-shape-dependent — not two opposite outcomes.** `business_model` works as a cohort key when the cohort IS its primary-tag semantics (pure-play metered → clean), and fails when the cohort cuts across models (SaaS leaf too coarse → collapse) or is tagged by primary leg only (hybrids → missed). A queryability guardrail (know the cohort's structural uniformity before keying on the field), distinct from the n=4 industry-draw `denominator-reconciliation` pattern. | That it contradicts run-039 — it's a boundary condition on the same behavior; only that field-as-key reliability is a cohort-shape property, not a field-quality property. | developer-review.md; read.md Result(3); run-039 G1; run-044 G2 | denominator-reconciliation, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | G1 | gap | **No structured field isolates the creative-agency cohort.** `offering_category [Services / Consulting]` matches **80** profiles (carried store-wide as a *secondary* clinical-services value across telehealth + VC firms + usertesting/warbyparker); `business_model Services / Project-based` matches **8** — the 5 agencies + 3 non-agency contaminants (euclidpower energy, verdegoaero aerospace mfg, goinfusive healthtech). A buyer who doesn't already know the 5 names cannot draw them from frontmatter. **New, more severe flavor of denominator-reconciliation:** the n=4 industry-draw (036 G3/037 G2/039 DR1/042 G3) was a roughly-right draw *contaminated*; here **no field even approximates the cohort.** | That the fields are mis-tagged — each is individually correct; only that *creative agency* has no isolating structured handle. | read.md Result(1), C1/C2; grep counts 80 vs 8 | denominator-reconciliation, schema-edge-entity-type |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | G2 | gap | **Within-cohort structured degeneracy *for buyer differentiation*.** The 4 *typing* fields are identical across all 5 (`entity_type Company` · `offering_category [Services / Consulting]` · `business_model Services / Project-based` · `primary_industry Consulting & Professional Services`); the 2 that vary (`target_market`: IDEO `[B2B, B2G]` vs rest `[B2B]`; `portfolio_shape`: 3 values) vary in **buyer-irrelevant** ways. The closed `offering_category` set is **taxonomy-bottomed-out** at `[Services / Consulting]` — no finer leaf in TAXONOMIES.md. Sharper than run-039's SaaS flattening (`[Software / SaaS]` sat above ~19 sub-markets, finer leaves conceivable). [Corrected per VR1/DR1 from an "identical 5-field tuple" overreach.] | That a finer enum value should be added — n=5, single cohort; prose differentiates them. Only that the structured layer can't, for the buyer's purpose. | read.md Result(2), C3; 5 profiles' frontmatter; run-039 G1 | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | G3 | gap | **The buyer's #1 differentiator — firm scale/shape (solo→boutique→global) — has no structured field.** The cohort spans the whole range (IDEO 5 global studios → Heco 2 partners → Parlance 1 person + EA). `portfolio_shape` is degenerate for it: **3/5** (IDEO, Heco, Parlance) all carry `Flagship + companions` — it encodes service-line structure, not firm size. Scale lives only in prose. | That a firm-scale field should be built — it'd be a rotting captor judgment, mostly-blank store-wide (fails engine-dev's fillable-cut bar). Only that the most decision-relevant axis is unstructured. | read.md Result(3); portfolio_shape across 5; ideo:44/heco:39/parlance:38 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | S1 | surprise | **Prose carries the full buyer decision — richly — for all 5.** Capability (`What they offer`), specialization (`Positioning & audience`), proof/named clients (`Credibility & proof`), and engagement model (`How it works`) are all present and well-organized; a human can shortlist + differentiate cleanly. The **buyer-value inverse** of the schema-fit failure — continues the run-042 S2 / run-043 S1 "store is a genuine strength for the reader" thread on the entity type that most defeats structure. | That the schema succeeds — it fails structurally (G1–G3); only that the *prose layer* serves the buyer well. | read.md Result(4); 5 profiles' body sections | query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | S2 | surprise | **The price-visibility token is faithful on services — `[on-request]` here means no list price *exists* (custom-scoped), not a hidden/gated one.** Unlike a DTC brand withholding a real price, an agency's bespoke scope has no list price to withhold, so the schema's pricing silence is honest market structure. The token's "can I even get a price?" axis (SCHEMA 2.3) generalizes correctly onto the lowest-price-surface entity type. Cleanest case yet for engine-dev's "evidence, not a score/field." Extends L006's sharpened scope (3rd entity-type confirmation: 037 DR3 / 044 S3 / now 045). | That the token is the only pricing answer a buyer needs — a buyer still wants a budget anchor (CR1); only that the token reads *correctly* here. | read.md Result(5); SCHEMA.md:142; parlance:62-66; lessons.md L006 | source-rigor, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | S3 | surprise | **Parlance is the lone price-transparent member and the token captures it — but only at body-line grain.** Sprints "$15k" / Office Hours "$225" / Mentorship "free" are `[published]`; bespoke fractional/advisory `[on-request]`. The other 4 uniformly `[on-request]`. The token correctly surfaces the split, but it's a `What they offer` body token with **no frontmatter roll-up** — a buyer filtering frontmatter for "which agency publishes any price" wouldn't see it. **Echo of run-044 G1** (token uneven/invisible as a cross-cohort surface). | That a frontmatter price-visibility scalar should be built — SCHEMA explicitly makes it per-offering, never a company scalar. Only that the split is invisible to a structured filter. | read.md Result(5), C5; parlance:62-66; SCHEMA.md:142; run-044 G1 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | S4 | surprise | **The design-agency-specific visual read is already in profile.md, not gated behind visual.md.** `visual.md` exists for only 2/5 (bullish, parlance), but every profile carries a rich `Visual & brand impression` section — and for a *design* agency (where the site IS the portfolio piece), that section is a genuine buyer ingredient. So the uneven `visual.md` coverage doesn't block the shortlist. | That visual.md is redundant — it adds blind-card depth profile.md compresses; only that the buyer-load-bearing visual read is in profile.md for all 5. | read.md Gap Map; profile `Visual & brand impression` x5; visual.md present 2/5 | coverage-caveat |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | W1 | wish | If anything ever graduated from G1/G2, the lightest path is **not a firm-scale field** but acknowledging the closed `offering_category` set lacks an *agency/specialization* value (it bottoms out at `[Services / Consulting]`) — and even that only *if* a real **filtering/programmatic** consumer needs to select agencies by specialization from frontmatter. A human reader needs neither (S1 is positive evidence). Mirrors run-036/037/039/042/043/044 anti-sprawl W1 landings. | That it should graduate now — only the lightest path *if* a 2nd pure-services cohort shows the same pattern AND a filtering (not reading) consumer appears. "No new primitive needed" stays live. | read.md What Would Change; .claude/rules/engine-dev.md | query-time-grouping-enough, schema-edge-entity-type |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | VR1 | risk-miss | (Evidence verifier) G2 in run-notes originally asserted "All 5 collapse to the identical tuple: target_market [B2B]" but IDEO is `[B2B, B2G]` (ideo:42), and `portfolio_shape` has 3 distinct values across the 5 (Flagship+companions / Single / Multi-product) — so "identical" was false on 2 of 5 fields. The structural degeneracy point survives (3 values for portfolio_shape still tell a buyer nothing about firm scale), but the framing overstated sameness. **Corrected in read.md Result(2) + run-notes G2/G3.** Evidence the 3-pass Loop 2 verifier catches a precision slip a single-pass read ships (as run-042 VR1). | That the finding is wrong — degenerate-for-buyer-differentiation holds; only that "identical tuple" was a precision overreach. | ideo:42; redantler:37 (Single); bullish:44 (Multi-product); run-notes G2 | source-rigor, schema-edge-entity-type |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | CR1 | gap | (Consumer review) Prose carries buyer-decision for shortlisting, but "rough price" is bundled into the carried-cleanly Gap Map column when **4/5 firms deliver zero price signal** — only the absence of a price. A buyer pre-qualifying by *budget* (not just capability) cannot shortlist from prose alone for four of the five. The Gap Map's "sufficient for a human shortlist" is right for capability/specialization but overstated for the budget-filtering step. | That prose fails the buyer or a price field is needed — `[on-request]` IS correct/honest for custom-scoped firms; only that the Gap Map conflates capability-shortlisting (prose sufficient) with budget-qualification (prose not, for 4/5). | read.md Gap Map row 3; ideo:66; heco:28; run-notes S1/S2 | query-time-grouping-enough, coverage-caveat |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | CR2 | friction | (Consumer review) Firm scale (G3) is the *first* screen a shortlisting buyer applies (budget-qualifying IDEO vs Parlance = global firm vs solo operator), but scale lives only in prose — requiring full sequential reads of all 5 profiles before the filter can fire. The read calls prose "sufficient for a human shortlist" (true) but understates the friction: the buyer front-loads the reading cost of all 5 before learning which subset merits careful reading. | That a firm-scale field should be built — run-notes W1 correctly argues it'd rot; only that prose-only scale imposes sequential full-read friction a structured indicator would remove. Anti-sprawl hold is correct; the friction cost is real and unnamed in the read. | read.md Result(3); run-notes G3; ideo:62; parlance:73; heco:111 | depth-backfill, query-time-grouping-enough |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | DR1 | gap | (Developer review) The read's portfolio_shape degeneracy evidence originally cited only IDEO + Parlance as the `Flagship + companions` pair (a 2-member coincidence). **Heco (2-partner boutique) also carries it** (heco:39) — a **3/5** case across three dramatically different firm scales (global / 2-partner / solo). The supporting evidence was understated; same conclusion, stronger than cited. **Corrected in read.md Result(3) + run-notes G3.** | That the finding is wrong — the degeneracy is real and survives; only that citing 2 members rather than 3 understated the evidence. | read.md Result(3); heco:39; ideo:44; parlance:38 | source-rigor, schema-edge-entity-type |
| 2026-06-24 | runs/045-2026-06-24-agency-services-schema-fit | DR2 | gap | (Developer review) W1's lighter-fix alternative ("the closed `offering_category` set lacks an agency/specialization value") is offered without testing whether *that* value would itself fail the fillable-cut bar. Distinguishing "creative/brand agency" vs "management consultancy" vs "dev shop" is a captor judgment — not site-derivable from a marketing page — making it roughly as unstable as the firm-scale field W1 correctly rejects. The anti-sprawl reasoning for the alternative path is incomplete against engine-dev's own bar. | That W1's graduation hold is wrong — "no new primitive needed" is the right call and W1 is explicit about holding it; only that the supporting reasoning for the lighter alternative has an unnamed gap. | run-notes W1; read.md What Would Change; .claude/rules/engine-dev.md; ideo:32/redantler:27/bullish:30 | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-24 | runs/046-2026-06-24-consumer-goods-transactional-buyer-read | W1 | wish | A catalog buyer's **non-price purchase-protection factors** — return window, warranty length, free-shipping threshold, FSA/HSA, BNPL, channel — are captured in all 4 transactional consumer-goods profiles but **only in prose** (`How it works`, `Credibility & proof`). No frontmatter or `offerings.md` field carries them, so a 4-brand purchase-protection table can't be drawn structurally; several cells read "not quoted." Same "body carries the decision, structured spine doesn't" shape as run 045 (agencies), now on a cohort whose *price* the spine **does** carry — isolating the gap to non-price retail factors. | That a field/block should be built — only that the want is real, cross-brand-comparable, and unfielded; n=4 is too thin to mandate a primitive (what would it replace?). | read.md Result(2)+Gap Map; C4; SCHEMA.md (no warranty/return field); store/{warbyparker,nike,therabody,hyperice}-com/profile.md | depth-backfill, schema-edge-entity-type |
| 2026-06-24 | runs/046-2026-06-24-consumer-goods-transactional-buyer-read | G1 | gap | `portfolio_shape: Catalog` deliberately means *un-enumerable*, so catalog breadth lives in `offerings.md`, whose **enumeration scope varies across the cohort**: Hyperice rostered all 65 handles (`Multi-product`); Therabody `indexed-complete`; **Nike enumerated only its recovery/wellness line** (rest parked in `unverified_fields`); Warby exemplars-only. A "who's widest" read is a coverage artifact, not a market fact. | That the offerings module is broken — only that cross-brand breadth is comparable solely where enumeration scope matches; structured/rostered absence ≠ market absence (L005 corollary). | read.md Result(3); C5; store/nike-com/profile.md unverified_fields; store/hyperice-com/offerings.md (65 handles) | denominator-reconciliation, coverage-caveat |
| 2026-06-24 | runs/046-2026-06-24-consumer-goods-transactional-buyer-read | S1 | surprise | The per-line price-visibility token (`[published]`/`[on-request]`) handled the **consumer-line-vs-services split cleanly across all 4** — each pairs a `[published]` hardware catalog with an `[on-request]` services/B2B leg (Warby exams, Therabody Reset/Coach/corporate, Nike free membership, Hyperice team sales) — needing no misleading company-level "transparent pricing" scalar. Expected the subscription-shaped schema to strain on transactional retail; the price/revenue fit was instead clean and the strain was entirely on non-price factors. | That this graduates anything — only that per-line-token-not-company-scalar (cousin to L006) corroborated on a fresh, non-intermediary entity type. | read.md Result(1); C3; SCHEMA.md:142 | query-time-grouping-enough |
| 2026-06-24 | runs/046-2026-06-24-consumer-goods-transactional-buyer-read | G2 | gap | Therabody + Hyperice were captured **mid-Prime-Day sale**; `offerings.md` prices are sale snapshots with strike-through regular prices ("re-check before ranking"). `captured_at` dates the capture but **carries no flag that the merchandising state was promotional**, so a returning buyer can't distinguish a sale snapshot from a regular-price one without reading the offerings note — a clean-looking captured price that is silently a sale price. | That a sale-state flag should be added — only that `captured_at` alone doesn't signal promotional pricing, and a price read off it can mislead. | read.md Source Gaps; C6; store/therabody-com/offerings.md site_notes; store/hyperice-com/profile.md unverified_fields | freshness-monitoring, source-rigor |

| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | G1 | gap | **Horizontal relation absent — 2nd-anchor confirmation.** Datadog's 5 named rivals (New Relic, Dynatrace, Splunk, Grafana, Elastic) live in **one prose line** (`profile.md:74`) with **no structured `competes-with`/substitute field** (frontmatter carries only the vertical `parent`/`owns`), and **0/5 are captured as nodes**. Every competitor edge dangles off-store. Confirms run-039 S1 (axis-asymmetric relation) + G2 (rivals off-store) on a fresh anchor + sub-market, removing the "maybe telehealth" residual. | That a competes-with field should be built — only that relation support is vertical-structured / horizontal-absent and the edge endpoints are uncaptured. | read.md Result(1); datadoghq-com profile.md:74/120; `ls store/` (0/5); run-039 S1/G2 | relation-pressure, query-time-grouping-enough |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | S1 | surprise | **The edge is the cheapest thing to source; the gap is node-coverage, not discoverability.** One SERP query + one listicle scrape recovered **all 5** store-named rivals and **extended** the set to 14 (cloud-native + enterprise + OSS). So "the store can't see competitor edges" is not a discovery problem — a directed edge-set for a well-known SaaS anchor is near-free to re-derive live. **Sharpens run-039 W1:** a stored `competes-with` field would dangle not because edges are hard to find but because the *rival nodes* aren't captured — doubly unmotivated (re-deriving is cheap AND endpoints missing). | That edges are always cheap — depends on the anchor's notoriety; a niche B2B tool may have no "alternatives to X" panel (a hardening condition, not a claim). | read.md Result(2)/(3); C1/C2; run-039 W1 | relation-pressure, query-time-grouping-enough |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | R1 | risk-miss | **`bounded-live-spend` recurrence — new flavor.** A single `firecrawl_scrape` with `formats:["json"]` (LLM extraction) billed **5 credits** where a plain-markdown single HTML page is ~1, taking gross spend to 7 — a **one-credit breach** of the 6 ceiling (net 6 after a 1-credit search refund). The plan was hardened against run-040's **PDF** cost but not the **JSON-extraction surcharge** — cost is invisible pre-call for this format too. Stopped before the 2nd planned scrape; conclusion already complete. | That the ceiling is wrong or the scrape was off-scope — the action was in-plan (single HTML listicle, no PDF, no funnel); only that JSON-extraction credit cost is invisible pre-call, so "stop before exceeding" is fragile for `formats:["json"]` exactly as for `parsers:[pdf]` in run-040. | run-notes header/exit-check; C2 receipt (creditsUsed 5); run-040 R1 | bounded-live-spend, source-rigor |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | S2 | surprise | **The recovering surface is vendor-authored content marketing.** The decision-grade panel is a competitor's own "alternatives to Datadog" listicle (SolarWinds ranks **its own** product #1; the New Relic blog is likewise a rival's post). A self-serving denominator — the rival *set* survives cross-source corroboration (≥2 independent listicles), but *rank and inclusion* carry vendor bias. L004 instance on the listicle/relation axis. | That the listicle is useless — the recovered *set* is reliable; only that vendor-authored rankings are a fallback panel, not a neutral neighborhood denominator. | read.md Source Gaps; C2 (SolarWinds self-#1); lessons.md L004 | source-panel, source-rigor |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | W1 | wish | If anything ever graduates from G1/S1, the lightest path is a **query-time recipe** — "for competitor edges, read the profile's prose `Competes with` line + one external alternatives-listicle panel (corroborate across ≥2 independent listicles; treat vendor rank as biased)" — **NOT** a stored `competes-with` relation field. Load-bearing reason: the edge is near-free to re-derive AND a stored field's endpoints dangle off-store today (fails engine-dev's fillable-cut bar). Mirrors run-039 W1 and the 036–046 anti-sprawl W1 landings. | That it should graduate now — only the lightest path *if* the rival nodes get captured AND a real cross-company neighborhood consumer appears. "No new primitive needed" stays live. | read.md What Would Change; run-039 W1; .claude/rules/engine-dev.md | query-time-grouping-enough, relation-pressure |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | CR1 | surprise | (Consumer review) **Value lands on the builder/Pantry, not the buyer.** A buyer's deliverable (the rival shortlist) came entirely from the external listicle — Truffle's contribution was *negative space* ("the store already names these 5"). The neighborhood is the run's Judgment from a fresh scrape, not store-queryable State — the same "map-not-ingredient" frontier as run-039 CR1, now on a 2nd anchor. Recurrence of the 038/039/041 CR1 builder-not-buyer frontier, after 043/044 broke toward buyer. | That the read failed — it's a gap-probe that lands its builder payload; only that the strongest value is not buyer-facing this run. | consumer-review.md Verdict; read.md Result(1)/(3); run-039 CR1; run-043/044 CR1 | coverage-caveat, query-time-grouping-enough |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | CR2 | wish | (Consumer review) **No M&A-freshness flag on the prose-named rivals.** Datadog's profile (capture 2026-05-31) names "Splunk", "New Relic", "Elastic" as current rivals, but in a consolidation market Splunk is now a Cisco product line and New Relic went private — a buyer-facing read would mis-read those edges as independent-vendor competition. The capture clock dates the *profile*, not the *named third parties'* corporate state. | That the names are wrong to use — they are page-attested rivals; only that a buyer-grade read needs an M&A-freshness note on major named rivals, which `captured_at` doesn't provide for third-party entities. | consumer-review.md value-diagnostics (Fresh enough); profile.md:74 | freshness-monitoring, relation-pressure |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | CR3 | gap | (Consumer review) **The neutral-denominator source family was never modeled, even as a documented alternative.** The run flags vendor bias (S2/L004) but the bounded plan only contemplated SERP + vendor listicles; a *buyer-grade ranked* neighborhood needs an analyst grid (Gartner APM MQ, Forrester Wave) — a distinct source family the plan didn't name. The gap is not just "we didn't fetch it" but "the plan didn't model what a neutral buyer-grade denominator would require." | That the run should have fetched it — bounded-live ceilings correctly precluded it; only that the buyer need + the missing neutral source family weren't named together. | consumer-review.md Job fit; run-notes live_evidence_plan | source-panel, coverage-caveat |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | VR1 | risk-miss | (Evidence verifier) read.md Result(2) originally said the rival set "is corroborated across ≥2 **independent** listicles (… New Relic blog …)" — but the corroboration is **snippet-grade** (only SolarWinds was full-scraped) and the New Relic blog is itself vendor-authored, so not independent. A confidence-grade overreach the adversarial verifier caught; **corrected in read.md**. The rival *set* survives (Dynatrace/New Relic/Splunk/Grafana recur in non-vendor snippet leads). Same verifier-catches-a-precision-slip value as run-042 VR1 / run-045 VR1. | That the finding is wrong — the set holds; only that "≥2 independent, decision-grade" overstated snippet-grade corroboration that counted a vendor blog. | read.md Result(2) pre/post correction; C1 receipt (snippet_only: yes) | source-rigor |
| 2026-06-25 | runs/047-2026-06-25-saas-competitor-edge-external-recovery | DR1 | risk-miss | (Developer review) **Sharper framing of R1 — the spend guardrail gap is a *class*, not two instances.** Run-040 (PDF) and run-047 (JSON-extraction) are both members of "Firecrawl formats that delegate post-fetch processing and so carry a variable, pre-call-invisible per-unit cost." The `live_evidence_plan` `fail_closed_when` block excludes PDF explicitly but not the class, so the next plan-writer hardens against "PDF and JSON," not the family — and a third variable-cost format will breach again. The grep-verifiable fix is a class-level rule ("disallow post-fetch variable-cost formats under a light ceiling"). | That a plan change is due now — out-of-band learning-pass call; only that R1 names the instance and DR1 names the class the guardrail actually needs. | developer-review.md Dev Agent lens; run-040 R1; run-047 R1 | bounded-live-spend, tooling-ergonomics |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | G1 | gap | **Traction is structurally unanswerable from static State.** Every capital/order/milestone figure in the 8-company deep-tech cohort is a *level at one capture* (all `captured_at: 2026-06-14`); with no second capture to diff, "is the pipeline growing / fundraising accelerating / who's gaining ground" has no answer. The store supports a *size/stage* ranking, never a *velocity/momentum* one — the traction frame's #1 grain trap (level≠delta) hit head-on. **2nd `traction-readiness` sighting after run-029** (which was a store-wide coverage census; this is the cohort-momentum read). | That a momentum field/layer should be built — n=8, single cohort; only that the comparative axis the traction frame defers is the load-bearing absence; "no new primitive needed now" stays live (no cohort-momentum consumer yet). | read.md Result(2)/Gap Map; C2; all 8 `captured_at`; run-029 | traction-readiness, freshness-monitoring, query-time-grouping-enough |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | G2 | gap | **`0/8` of the cohort has any `signals/` capture** (C1) — the engine's comparability machinery (`signals/` append layer + `signal_delta.py`) was never run on deep-tech (telehealth-only to date). So the gap blocking a momentum read is **not capture** (levels are present and capturable) but the missing **time-series home** (traction frame #3) and **comparability** (frame #2). Locates the traction roll-up's (#4) blocker precisely. | That `signals/` should be run on this cohort — spend/approval-gated; only that its absence is exactly what makes momentum impossible, and the missing piece is comparability/durable-home, not capture. | read.md Result(2)/Source Gaps; receipt C1; run-029 | traction-readiness, source-panel, coverage-caveat |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | S1 | surprise | **The cheap, first-party, auditable traction anchor exists in the cohort yet is uncaptured.** beta-team is a public NYSE filer (ticker `BETA`, 2025 IPO >$1B, Q1 2026 10-Q) — exactly the "capture easy, obvious, first-party signal" the traction frame says to grab and refuse the paid-data swamp for — yet has **no `signals/` capture**. So the gap for this cohort is **not discoverability** (the single most reliable signal in the set is in plain view) but that the cheap-capture floor was never run. | That the store should capture it — only that discoverability is not the bottleneck for the public-filer case; the floor is just unrun. | read.md Result(3); beta-team profile.md:123; receipt C1 | traction-readiness, source-rigor |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | S2 | surprise | **Static State carries two loosely-correlated level axes — and they disagree.** A *stage* ordering re-derives run-042's maturity read, but a *capital* ordering does **not**: cfs raised the most ($2B cumulative) yet ranks near the bottom on maturity (zero power, pre-demonstration), while beta-team is high on both. So "traction-from-State" does not cleanly collapse onto "maturity-from-State" — there are ≥2 partly-independent level axes; what unifies them is that **none carries a delta**. [Corrected per VR1 from a "collapses onto maturity" overreach.] | That capital and maturity are unrelated — they correlate loosely; only that capital-raised is not a maturity proxy (cfs is the counterexample), so traction can't reduce to one ordering. | read.md Market Pattern/Result(1); run-042 maturity; cfs-energy profile.md:61,109; C2 | traction-readiness, query-time-grouping-enough |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | G3 | gap | **Cross-cohort capital is unit-incommensurable.** cfs "$2B cumulative" vs sora "$14.6M single round" vs blue "$380M" vs verdego/euclid (no $ at all); demand signals span pre-orders (electra 2,200) / backlog (beta 800) / binding orders (evoloh 500MW) / signed intent (16GW) / offtake LoI (sora). No common unit to sort momentum or even size cleanly. Cousin of run-023/043/044 price-incomparability, now on the funding/traction axis. | That a normalized capital field should be built — unit-incommensurability would launder false precision (engine-dev "evidence, not scores"); only that the magnitudes don't compare. | read.md Result(1)/Gap Map; C2; run-023/043/044 | denominator-reconciliation, source-rigor |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | S3 | surprise | **The capture clock can't date the traction *event*.** Recent events (blue $380M Apr 2026, sora $14.6M Apr 2026, euclid acquisition Apr 2026) and stale ones (electra Series B Apr 2025, cfs $2B undated) all sit under the same `captured_at: 2026-06-14`. Event recency lives only in prose milestone blocks, never a structured field — so "what moved lately" needs per-profile prose reading, not a query. Cousin of run-047 CR2 (capture clock dates the profile, not the named third-party/event state). | That a structured event-date field should be built — only that `captured_at` is per-profile freshness, not per-event recency, so it can't carry a "what's new" read. | read.md Result(2)/Gap Map; all 8 `captured_at`; run-047 CR2 | freshness-monitoring, source-rigor |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | W1 | wish | If anything ever graduates from G1/G2, the lightest path is a **query-time recipe + the cheap first-party capture** (ticker/10-Q for public filers; dated funding-news for the private 7) — **NOT** a normalized traction-magnitude field, which unit-incommensurability (G3) would turn into laundered false precision. Load-bearing reason: the comparative read needs a *second dated capture*, not a new field on the static snapshot. Mirrors the 036–047 anti-sprawl W1 landings. | That it should graduate now — only the lightest path *if* a real cohort-momentum consumer AND the comparability machinery (2nd capture + delta) appear. "No new primitive needed" stays live. | read.md What Would Change; .claude/rules/engine-dev.md; _design/2026-06-14-traction-frame.md | traction-readiness, query-time-grouping-enough |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | VR1 | risk-miss | (Evidence verifier) read.md originally claimed the size/stage ranking "largely re-derives run-042's maturity read" and that "traction-from-State collapses onto maturity-from-State" (S2) — but **cfs-energy raised the most capital ($2B) while ranking near the bottom on maturity**, so capital-size and maturity orderings disagree. A precision overreach caught and **corrected in read.md** (Lead/Result(1)/Market Pattern) + S2. Core finding (levels yes, delta no) survives, sharpened to two level axes. Same verifier-catches-a-slip value as run-042/045/047 VR1. | That the finding was wrong — the no-momentum core holds; only that "collapses onto maturity" conflated two partly-independent level axes. | read.md pre/post; cfs-energy profile.md:61,109 vs beta-team:123 | source-rigor, traction-readiness |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | CR1 | gap | (Consumer review) **Value lands on the builder/Pantry, not the investor — "map-not-ingredient" on the traction axis.** The investor's literal ask (momentum) is the one thing the store can't ground; the buyer-facing deliverable is negative space ("size and stage yes, movement no"). Momentum is the run's own Judgment from a single snapshot, not a store-queryable ingredient. Same frontier as run-039/043/047 CR1, now on traction. | That the read failed — it's a gap-probe that lands its builder payload; only that the strongest value is not investor-facing. | consumer-review.md Verdict/Lens; read.md Lead | traction-readiness, query-time-grouping-enough, coverage-caveat |
| 2026-06-25 | runs/048-2026-06-25-deep-tech-cohort-traction-momentum | DR1 | surprise | (Developer review) **The traction bottleneck is *unrun capture*, not undiscoverable signal — sharpens S1.** For the public-filer case (beta-team) the cheapest, most-reliable traction signal (ticker/10-Q) is in plain view and free to capture; the engine simply never ran a Signal here. So the frame's #1 (capture easy first-party signals) is *available* for at least part of the cohort — what's missing is #2 comparability + #3 durable-home (run it twice, diff). Locates graduation work off "capture more" and onto "run the cheap floor + make it comparable." | That capture should start now — spend/approval-gated; only that discoverability is not the blocker for public filers, so "capture more data" is the wrong default framing. | developer-review.md Capability/Founder; beta-team profile.md:123; C1 | traction-readiness, source-panel, tooling-ergonomics |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | S1 | surprise | **The "render launders the flags" hypothesis is falsified at the artifact level.** `scripts/render.py` preserves every source-rigor flag in all 6 flag-heavy briefs: the full `unverified_fields` list renders as "What we couldn't verify" (li counts match source 3/3, 3/3, 2/2, 2/2, 4/4, 4/4), plus `site_notes` ("Field notes — read before trusting a number"), the self-reported proof prose, and the price-visibility token as styled roster chips (therabody 57 `published`; remedymeds 3 `published`+3 `on-request`). The one relay Truffle ships does **not** drop flag content — a positive against the 038/042/048 R-row "relay-dependent" worry. | That the brief is safe — content fidelity ≠ salience fidelity (see G1); only that no flag is dropped from the file. | read.md Result(1)/C1-C4; receipts/C1; scripts/present/brief.py:93-96,190-222 | source-rigor, query-time-grouping-enough |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | G1 | gap | **The recurring "relay-dependent" relay-risk is a *salience* property of the structured flags, not a content one.** In the shipped HTML brief the structured trust surfaces are uniformly off the default path: `unverified_fields`/`site_notes` render only in tab 4 ("Provenance & limits"), price-visibility chips only in tab 2, and the "self-reported" proof section renders collapsed; the default view is hero + tab-1 auto-open Overview/Strategic read. So the *structured* layer is **preserve-but-bury**. Locates where the 038-R1/042-R1/048-S3 "carry the flag downstream" dependency bites in the concrete artifact. [Scoped per VR1: the default path is not flag-free — captor prose often carries the caveat.] | That tab-4 placement is wrong — may be working-as-designed (flags = "limits," filed under Provenance); only that the *structured* flag surfaces sit off the default 5-second path. n=1 relay (the single-company HTML brief). | read.md Result(2)/Gap Map; scripts/present/brief.py:259-263,326-348; receipts/C1 (C2/C3) | source-rigor, query-time-grouping-enough, tooling-ergonomics |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | S2 | surprise | **The hero renders the most over-claim-prone field at peak salience with no flag of its own.** `description` is the single most prominent element (`hero-desc`, 21-29px); sorafuel's hero = "**Produces** sustainable aviation fuel…" (present-tense — the run-042 G1 maturity over-claim) while its *structured* guard ("captured pages describe a *planned* pilot… future production") sits in tab-4 `unverified_fields`. The relay puts the field that most needs a structural flag furthest from one (captor prose below may still rescue it — VR1). | That `description` is wrong to feature — it's the right lead field; only that featuring it without an adjacent structured maturity/self-reported flag reproduces run-042's over-claim at peak salience on the presentation surface. | read.md Result(2)/C1; sorafuel profile.md:96-98; scripts/present/brief.py:381 | source-rigor, schema-edge-entity-type |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | G2 | gap | **The renderer's stated intent and its salience disagree.** `brief.py`'s module docstring says the engine's trust surface (capture clocks, unverified fields, enumeration floors) is "rendered visibly **as the product**," but in practice the `unverified_fields`/`site_notes` surface is tab-gated (tab 4) and the proof section renders collapsed. A reader taking the docstring at face value would over-trust the brief's default-view honesty. | That the docstring is false — clocks *do* render in the hero eyebrow; only that "as the product" overstates the salience of the limits surface specifically. | scripts/present/brief.py:1-7 (docstring) vs :190-222,259-263 | source-rigor, tooling-ergonomics |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | W1 | wish | If anything ever graduates from G1/S2, the lightest path is a **salience tweak in `brief.py`** — surface one flag at hero grain (a "pre-revenue / pilot" or "figures self-reported" eyebrow beside `description`) or auto-open the limits section — **NOT** a new field/schema (the data already renders). Load-bearing reason: this is a presentation-layer Judgment, and the failure is ordering, not capture. Mirrors the 036–048 anti-sprawl W1 landings on the presentation surface. | That it should graduate or the brief should change now — only the lightest path *if* a real 5-second consumer (Scott Witt) is shown to mis-trust the default view AND the present-layer owner agrees tab-4 placement is the bug. "No new primitive needed" stays live. | read.md Gap Map / What Would Change; _design/2026-06-12-presentation-layer.md | query-time-grouping-enough, tooling-ergonomics |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | VR1 | risk-miss | (Evidence verifier) **The read's first framing — "the 5-second path contains zero flags" — was false and is corrected.** remedymeds' **auto-open** Strategic read carries "Scale and outcome figures are *self-reported and internally inconsistent… treat as marketing, not audited metrics*"; sorafuel's auto-open Overview/Strategic read carry "*venture-stage… moving toward pilot production… not a sales storefront… pre-commercial/pilot-stage.*" So the default path DOES carry a caveat **when the captor wrote one** — a captor-prose-dependent, unreliable second channel (run-037 DR2 shape), not the structural absence first asserted. Core finding survives, sharpened: *structured* flags tab-gated; *default-path* protection captor-prose-dependent. Same verifier-catches-a-slip value as run-042/045/048 VR1. | That the salience-inversion finding is wrong — the structured-flag burial holds; only that "5-second path is flag-free" overstated it by ignoring captor prose in the auto-open sections. | read.md Result(2) pre/post; remedymeds profile.md:120 (## Strategic read, open); sorafuel profile.md:51-53/118-120; brief.py:259-263 | source-rigor, schema-edge-entity-type |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | CR1 | gap | (Consumer review) **Value lands on the builder/Steward, not the end reader — map-not-ingredient on the presentation surface.** The deliverable is "here is where the shipped brief buries the structured trust flags," which the present-layer owner consumes; a buyer reading one brief gets no new company fact. Same builder-not-buyer frontier as 038/041/047/048 CR1, now on the brief renderer itself. | That the read failed — it's a gap-probe that lands its builder payload; only that the strongest value is not buyer-facing. | consumer-review.md Verdict; read.md Result | query-time-grouping-enough, tooling-ergonomics |
| 2026-06-25 | runs/049-2026-06-25-render-brief-flag-fidelity | DR1 | surprise | (Developer review) **The fix-shape, if anything ever graduates, is one-file and grep-verifiable — not a primitive.** The salience finding lives entirely in `scripts/present/brief.py` ordering (hero `description` vs tab-gated structured flags); the lightest response is a layout tweak, no field/schema/module. The anti-pattern to refuse is treating salience as a capture/schema problem or adding a "flag-salience" config knob — it's a single presentation Judgment downstream of State. | That a fix is due — out-of-band/Brian's call; only that the disposition is "one-file ordering Judgment," not "new primitive." | developer-review.md Dev Agent/Founder; brief.py:259-263,381; .claude/rules/engine-dev.md | tooling-ergonomics, query-time-grouping-enough |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | G1 | gap | **The store's most-loaded category token is a residual catch-all when read as a primary token in isolation.** `Services / Consulting` is the primary `offering_category` for 61 of 145 profiles and appears anywhere in ~82 — over half the store — yet it merges ≥6 non-comparable jobs/buyers: DTC telehealth care-wrappers (52), creative/brand agencies (5: ideo, redantler, bullish, heco, parlance), VC partnership (lsvp), B2B telehealth infra (openloop), membership primary care (onemedical), renewable-energy project services (euclid). A cross-store cut keyed on the lead token alone merges IDEO + Lightspeed + henrymeds + Euclid into one "category." | That a new sub-category/field is needed — the load-bearing telehealth/non-telehealth cut already exists in State (see S1); only that the *primary token alone* over-merges. | read.md Result/Gap Map; receipts/C1 | schema-edge-entity-type, denominator-reconciliation, query-time-grouping-enough |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | S1 | surprise | **The discriminating signal lives in the offering_category *pair*, not the primary token — but only at the first cut.** Presence/absence of a `Biotech / Pharma` secondary splits the 61 primaries 52 (telehealth wrapper) / 9 (professional services) with 100% clean separation in-sample (both directions); `entity_type: Investor / Holding` then flags VC. So `Services / Consulting` primary does two unrelated jobs at once — a telehealth-cohort *convention* AND a catch-all *residual* — which is exactly why reading it alone over-claims. [Scoped per VR1: the pair peels the telehealth mass off but does NOT discriminate within the 9-firm residual.] | That the pair is a universal discriminator — clean for the telehealth/non-telehealth cut only; a mis-tagged Biotech secondary would break it (none found). | read.md Result/Market Pattern; receipts/C1 | query-time-grouping-enough, schema-edge-entity-type |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | G2 | gap | **The bucket denominator is counting-method-sensitive — including a naïve-parse trap.** Four defensible counts disagree: 61 (primary/first-array, comment-stripped), **68 (naïve first-element parse — wrong)**, ~82 (token anywhere in array), 73 (raw exact-token frequency). The 68-vs-61 gap is a footgun: 7 telehealth wrappers (brello, fountain, goodlife, hims, keeps, rexmd, rugiet) carry inline `# STRAIN`/qualifier comments on the frontmatter line that break naïve `[`-then-`,` splitting. A "how many Services/Consulting companies" headline is meaningless without naming the counting rule + comment-stripping. | That any one count is "the" number — only that the figure depends on a stated rule (L004 in miniature, on the array-parse side). | receipts/C1; read.md Gap Map (per VR1); VR1 V1 | denominator-reconciliation, source-rigor |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | S2 | surprise | **The captor's "why this token" rationale is unevenly machine-readable.** Some profiles carry an inline `# STRAIN`/qualifier comment explaining a contested classification (firstround, sequoiacap, clerky, warbyparker, niagenplus, hims, keeps); most don't. So the rationale for the over-merge-prone telehealth-wrapper convention exists as prose where it exists at all — there's no uniform structured rationale surface, making the over-merge risk (G1) invisible to a structured query that doesn't read prose. | That a structured rationale field should be built — only that STRAIN-comment coverage is partial, so absence of a comment ≠ absence of strain. | read.md Missing/Stale; several profile.md `# STRAIN` lines | source-rigor, schema-edge-entity-type |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | W1 | wish | If anything ever graduates from G1/S1, the lightest path is a **one-line reading convention** in QUERYING.md or TAXONOMIES.md ("never group/compare on the `Services / Consulting` primary token alone; read the offering_category pair + entity_type") — **NOT** a sub-category, field, or stored object (the data to discriminate already renders). Load-bearing reason: the failure is *reader over-merge*, not missing capture. Mirrors the 036–049 anti-sprawl W1 landings (cut in State; fix is a recipe). | That it should graduate now — only the lightest path *if* a real downstream consumer is shown to be materially burned. "No new primitive needed" stays live. | read.md Gap Map / What Would Change; .claude/rules/engine-dev.md | query-time-grouping-enough, tooling-ergonomics |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | VR1 | risk-miss | (Evidence verifier) **The read's "the pair discriminates cleanly / counterexample none found" was an overreach — corrected in read.md.** Within the 9 non-telehealth primaries, **6 collapse to the identical `(Company, [Services / Consulting])` pair** (ideo, onemedical, redantler, bullish, heco, parlance) and 2 share `(Company, [Services / Consulting, Software / SaaS])` (euclid, openloop) — non-comparable jobs the pair + entity_type cannot separate. The pair peels the 52 telehealth wrappers off the residual but does NOT discriminate *within* it. Core finding (residual catch-all; no new primitive) survives, sharpened. Same verifier-catches-a-slip value as runs 042/045/047/048/049 VR1. | That the finding is wrong — the catch-all + no-new-primitive core holds; only that "clean discriminator / none found" overstated the pair within the 9-firm tail. | read.md Result/Gap Map/What-Would-Change pre/post; group-by over the 9 primaries; receipts/C1 | source-rigor, schema-edge-entity-type |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | CR1 | gap | (Consumer review) **Value lands on the builder/Steward, not the end buyer — map-not-ingredient, now the first *inward fold*.** The deliverable is "the dominant existing token over-merges; read the pair." Prior CR1 instances (038/039/041/047/048/049) found builder value in *new* schema edges or source families; 050 is the first to fold the problem onto the store's highest-traffic existing token — a read-convention failure that is structural (~56% of the store), not marginal. The data is already a correct farm; the label on the most-used bin misleads a reader who reads only the label. | That the read failed — it lands its builder payload; only that the strongest value is architect-facing, and this sharpens (doesn't nullify) the recurring frontier. | consumer-review.md Verdict/CR2; read.md Market Pattern | query-time-grouping-enough, schema-edge-entity-type, coverage-caveat |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | CR2 | surprise | (Consumer review) **The 52/9 Biotech-secondary pair-key is a strong, buy-ready ingredient — the gap is persistence, not data quality.** A Pantry consumer building a telehealth roster or stripping non-telehealth firms can use it today (100% clean in-sample); the recipe is trivial once known. The outstanding gap is that the recipe is surfaced by the run but not persisted in QUERYING.md, so each consumer must re-derive it. | That it's a failure — it's a positive about existing store structure; only that recipe persistence, not capture, is the missing step. | consumer-review.md CR5/Pantry lens; read.md Result C2 | query-time-grouping-enough, tooling-ergonomics |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | DR1 | surprise | (Developer review) **A genuine 2nd-sighting for L005 with a *new grain*: the grouping key must be the full array, not element[0].** Prior L005 sightings (015/027/028/031/032/035) tested whether a grouping already carries a cut; 050 applies it to the highest-traffic token and finds the cut exists but only in the array *pair*, not the lead element — an array-positional variable the current L005 rule text doesn't name. | That L005's rule text should change now — out-of-band/Brian's call; only that this sighting adds a concrete array-position instance. | developer-review.md Founder; lessons.md L005; read.md Result | query-time-grouping-enough, schema-edge-entity-type |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | DR2 | risk-miss | (Developer review) **The over-merge is a live reading hazard on `offering_category[0]`; urgency hinges on an un-run audit.** If 52/145 profiles (36%) use the primary token as a *convention* not a literal category, any tool/recipe/render script keying on `offering_category[0]` silently misclassifies them. The run names the hazard but did NOT audit whether an *existing* QUERYING.md recipe or `scripts/present` surface already does this — that audit decides whether the fix is urgent or deferrable. | That a fix is due now — only that the deferred-fix disposition (W1) should carry an "audit existing recipes for [0]-keying" check before it's called safe. | developer-review.md Query/access + Dev Agent; read.md Market Pattern; W1 | tooling-ergonomics, schema-edge-entity-type, source-rigor |
| 2026-06-25 | runs/050-2026-06-25-services-consulting-bucket-overload | DR3 | gap | (Developer review) **The telehealth-wrapper pair convention is undocumented in the schema — a maintenance-rot risk — and the L006 link is looser than the run first claimed.** The `Services / Consulting` + `Biotech / Pharma` cohort-tag convention lives only as a corpus pattern, not in SCHEMA.md/TAXONOMIES.md; a new capturer wouldn't know to replicate it, so the pair-discriminates rule could silently rot. Separately, L006 (price-visibility token *meaning* grain on two-sided entities) and this run (one token carrying two *unrelated* conventions) share a surface but differ in mechanism — a learning pass should keep them distinct, not log this as a clean L006 2nd-sighting. | That the convention must be formalized as a field, or that L006 is irrelevant — only that the undocumented convention is a distinct maintenance risk and the two mechanisms shouldn't be merged. | developer-review.md Capture/Founder + DR3; lessons.md L006; read.md Market Pattern | schema-edge-entity-type, source-rigor |

| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | S1 | surprise | **First head-on read of the "cold-start a company" value job — cold-start is structure-shaped, not brand-shaped.** A reproducible 6-company sample spanning Investor/Holding + deep-tech + telehealth + SaaS + luxury + smart-hardware, vintages 2026-05-30→06-24, schema 2.2→2.6, all carry the *same* 9-section skeleton (count+order uniform; only header case varies — airtable title-case, per VR1) + uniform classification frontmatter, and the 4 cold-start questions (what/who-for/how-monetizes/what-to-distrust) map ~1:1 onto it. The Scout contract's "reliability varies unpredictably by company" hypothesis is falsified at the structural level. Every prior run pre-curated a clean cohort, which *hid* this realistic single-unfamiliar-company case. | That cold-start always succeeds — peripheral-metadata depth varies by vintage (G1) and a fluent-but-unflagged profile could still trap (none in sample); only that the scaffold + trust surface are uniform across heterogeneous entities. | read.md Result table; header-skeleton grep across the 6; receipts/C1 | query-time-grouping-enough, confidence-grain |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | S2 | surprise | **`business_model` is the one lead *field* that breaks cold-start "how it monetizes."** The single-valued closed enum is thin/misleading for 3/6 (blueowl `Other`, eightsleep `Subscription` for a hardware+mandatory-sub hybrid, blueenergy `Usage-based / Consumption` for PPA power sales); `How it works/model` prose rescues all 3. A 4th milder case (agelessrx `Subscription` simplifies a sub+one-time-diagnostics mix, prose flags it) makes the split a gradient. **3rd entity-set sighting of single-valued-`business_model` lossiness** after run-037 S1 (wearable hybrids) and run-044 G2 (metered hybrid), now on the cold-start-reader lens. | That the field is broken or a field is needed — single-valued is a deliberate contract choice and prose rescues it; only that the fast-read field mis-serves non-standard monetizers for a frontmatter-only cold reader. | read.md Result(detail); blueowl profile.md:35; eightsleep:44; blueenergy:39; run-037 S1; run-044 G2 | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | S3 | surprise | **"What to distrust" is the *best-served* cold-start question, not the worst** — all 6 `unverified_fields` blocks are non-empty and name the load-bearing soft spot precisely (blueowl AUM self-reported; alange parent inferred from footer; agelessrx prices point-in-time + rotating coupons, true cost behind portal; eightsleep "4th July Sale" snapshot + Pod 5 Ultra price missing; blueenergy PPA pricing + legal entity absent). Independent verifier confirmed all 6 non-empty. The cold reader is structurally protected from over-trust — *only if they read the block*. Relay/salience-dependence (038-R1/042-R1/049-G1) confirmed on the raw `profile.md` cold-start surface (where it is mildest; bites harder on the rendered brief). | That protection is automatic — it depends on the reader reading the flag block, which nothing on the path forces; only that the *content* is present and honest in all 6. | read.md Result(detail); the 6 `unverified_fields` blocks; VR1 | source-rigor, query-time-grouping-enough |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | G1 | gap | **Schema-vintage drift leaves peripheral-metadata holes but a version-robust cold-start core.** The two oldest captures (blueowl, alange — schema 2.2) lack the `socials`/`legal_entity`/`logos`/`modules` blocks the 2.5/2.6 captures carry (verifier-confirmed); but `description`, `Overview`, `unverified_fields`, and `Strategic read` are present in *all* 6 versions, so cold-start quality is vintage-robust. | That older captures fail cold-start or need backfill — the cold-start core is intact across versions; only that the newer identity modules are absent on 2.2-vintage profiles. | read.md Gap Map; blueowl/alange (2.2) vs others (2.5/2.6) frontmatter | coverage-caveat, depth-backfill |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | S4 | surprise | **Cold-start quality did not track reader prominence/priors.** The obscure deep-tech (blueenergy) and asset manager (blueowl) profiles were as legible on the 4 cold-start questions as the familiar airtable/alange/eightsleep — evidence the read is a property of the capture contract, not a reader-prior artifact. Honest limit: an LLM reader carries broad priors and is not a true blind cold-starter, so this is suggestive, not proven. | That the reader is truly blind — an LLM has priors on famous brands; only that legibility tracked profile structure, not brand fame, in this sample. | read.md What Would Change; receipts/C1 prominence-mix column | confidence-grain, coverage-caveat |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | CR1 | surprise | (Consumer review) **Value lands on a real end-reader (the cold-starter), not the builder — and the store *serves it well*.** The cold-start job is the inverse of the recurring "store falls short for the buyer / map-not-ingredient" CR1 frontier (038/039/041/047/048/049/050): here Truffle's per-company profile is a genuine strength for the consumer, the 2nd consecutive register after 043/044 where a real consumer gets the payload. The cold-start *verdict* is still the run's Judgment (a map), but the map is consumer-facing and the underlying State is the reusable ingredient. | That the builder-not-buyer frontier is wrong — it recurs for cohort/relation/momentum reads; only that the cold-start job specifically is one the store serves well for an end reader. | consumer-review.md Verdict/CR1; read.md Market Pattern; run-043/044 CR1 | query-time-grouping-enough, confidence-grain |
| 2026-06-25 | runs/051-2026-06-25-cold-start-profile-reliability | VR1 | risk-miss | (Evidence verifier) **6th consecutive run where the adversarial verifier caught a precision slip.** read.md originally called the 9-section skeleton "identical" across all 6, but airtable uses title-case headers (`## What They Offer`…) where the other 5 use sentence case — "identical" is falsifiable by a header diff. Caught and **corrected** to "same (count+order uniform; header case varies)". The structural-uniformity finding (S1) survives intact. Also a soft note: agelessrx `business_model: Subscription` is a mild simplification too (folded into S2 as a gradient). Same verifier-earns-keep value as run-042/045/047/048/049/050 VR1. | That S1 is wrong — the structural uniformity holds; only that "identical" overstated exact-string sameness by one profile's header capitalization. | read.md Result pre/post correction; airtable profile.md:69/79/85/91/117/126/130 | source-rigor, schema-edge-entity-type |

| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | G1 | gap | **First empirical test of the 041/048 "captured State has no change-detection home" gap.** Of 3 re-checked panel prices, the only divergence was Peloton's refurb Original Bike, where a **dated promo with a printed expiry** ("$695… ends June 15, 2026", captured 06-10) had lapsed by re-check (06-25) and reverted to the struck regular price **$1,145**. The predictive datum — the promo end date — is captured in `offerings.md` prose / `site_notes` but lives in no frontmatter or price-cell field, so a reader/monitor can't *query* "which captured prices sit on a promo window that has already passed." | That a structured promo-expiry field should be built — n=1 diverging price, prose carried the expiry every time; only that the decay was promo-bound and the predictive datum is unstructured. | read.md Result(1)/Gap Map; receipts/C1 C3; store/onepeloton-com/offerings.md:7/44/57; runs 041/048 | freshness-monitoring, depth-backfill, query-time-grouping-enough |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | S1 | surprise | Captured *promotional* prices did **not** spontaneously rot at 1-day age, and a captured dated sale stayed valid **inside its own printed window**: Oura's "Flash Sale through June 26th / From $244" (captured 06-24) was still live and still $244/$279 on 06-25. A captured promo price isn't self-evidently stale — its trustworthiness is bounded by the promo's own end date (often captured verbatim), not by bare `captured_at`. | That promo prices never rot fast — they revert the moment their window passes (G1); only that within-window they held, so `captured_at` alone over- and under-states staleness depending on the promo window. | read.md Result(2); receipts/C1 C1; store/ouraring-com/offerings.md:32/47 | freshness-monitoring, source-rigor |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | S2 | surprise | The one price that diverged was **already self-flagged in its own capture** — the 06-10 Peloton capture recorded both the struck regular price ($1,145, = today's live price) and the verbatim expiry ("ends June 15, 2026"). The store held everything needed to predict the rot **without any live fetch**; the live re-check only confirmed what a careful read of `unverified_fields`/`site_notes` already implied. | That the live fetch was unnecessary in general — it falsified the alternative (unpredictable drift) and caught G2; only that for promo-bound decay the capture's own prose was sufficient to predict it. | read.md Result/Missing-Stale; store/onepeloton-com/offerings.md:44/45 | freshness-monitoring, source-rigor, query-time-grouping-enough |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | G2 | gap | A "live re-check" via `firecrawl_scrape` silently returned a **cached scrape dated 2026-06-24** for Eight Sleep (cacheState `hit`, cachedAt 06-24) — the same day as the original store capture — while Oura returned fresh-today and Peloton was a cache `miss`. A freshness-verification routine that doesn't bust the fetch cache can re-confirm stale data as "still current," defeating the check. | That the tool is broken or the price was wrong — Eight Sleep's price matched anyway; only that cache behavior makes "I re-checked it live" unreliable unless cache-busting is explicit (`maxAge:0`). | read.md Result(3)/G2; receipts/C1 S2 metadata (cachedAt 2026-06-24T02:05Z) | tooling-ergonomics, source-rigor, freshness-monitoring |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | S3 | surprise | A vendor-side internal inconsistency was itself **durable** across capture→re-check: Peloton's stale Affirm footnote (refurb Bike+ "based on a price of $1,995" vs displayed $1,395), captured + flagged on 06-10, persisted unchanged on the live 06-25 page. A captured `unverified_fields` discrepancy can be a stable property of the live site, not a capture-time artifact. | That the footnote will never be fixed — only that a flagged on-site inconsistency reproduced live 15 days later, so the 06-10 `unverified_fields` flag is still accurate. | read.md Source Gaps/What-Would-Change; store/onepeloton-com/offerings.md:45; receipts/C1 C4 | source-rigor, freshness-monitoring |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | VR1 | risk-miss | (Evidence verifier) read.md finding (1) originally stated "the only price that rotted was a promo with a printed expiry" **without scoping to the 3 re-checked brands** — Therabody + Hyperice (both captured mid-"Prime Day," stop-ruled out at 3) have unverified sale-persistence. A positive "only" claim that overstated coverage; **corrected in read.md** to "Of the 3 re-checked brands." Core finding (promo-bound decay) survives. Continues the 042/045/047/048/049/050/051 streak of the adversarial verifier catching a precision slip. | That the finding is wrong — promo-bound decay holds for the 3 re-checked; only that "only" overstated the verified scope by counting 2 un-rechecked brands. | read.md Result(1) pre/post; run-notes Evidence-limits; receipts/C1 Limits | source-rigor, freshness-monitoring |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | CR1 | gap | (Consumer review) **Value lands on builder/Steward, not the end buyer — recurring CR1 frontier, now with a concrete-close shape.** The consumer is the Steward who learns "promo-bound decay is predictable from existing prose without a live fetch." A buyer wanting a current price gets nothing actionable. Unlike prior CR1 instances, the builder-side value has a specific actionable closing path (a promo-expiry grep recipe, zero live spend). | That the run failed or the CR1 frontier is solved — only that the payload is builder-facing and the concrete close is a recipe-persistence step, not a structural gap to fill. | consumer-review.md Verdict; read.md Market Pattern / What Would Change; runs 038/039/041/047/048/049/050 CR1 | freshness-monitoring, query-time-grouping-enough |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | CR2 | surprise | (Consumer review) **The store held everything needed to predict the decay without the live fetch.** Peloton's 06-10 capture recorded both the verbatim expiry and the post-promo regular price ($1,145); the live re-check only confirmed what `offerings.md` already implied. For promo-bound decay the live fetch adds confirmation but not discovery — the capture is self-sufficient for identifying its own risk, if a reader knows to look. (Sharpens S2 from the consumer lens.) | That live fetches are unnecessary in general — the fetch falsified unpredictable-drift and caught G2; only that for this decay mechanism the capture's own prose was the load-bearing predictive datum. | consumer-review.md Verdict; read.md S2/Result(1)/Missing-Stale; store/onepeloton-com/offerings.md:44/45 | freshness-monitoring, source-rigor, query-time-grouping-enough |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | CR3 | gap | (Consumer review) **The cache-hit makes the Eight Sleep "unchanged" cell trivially same-day — not an independent freshness verification.** The Eight Sleep match is therefore not an independent data point for the decay question, only a confirmed artifact of the capture-day scrape; a freshness-check routine that doesn't bust the cache can re-confirm stale data as current. (Consumer-lens re-sighting of G2.) | That the price is wrong or the tool is broken — it matched regardless; only that cache behavior makes "live re-check" unreliable for freshness verification without explicit `maxAge:0`. | consumer-review.md Verdict; read.md G2/Result(3); run-notes G2 (cachedAt 2026-06-24T02:05Z) | tooling-ergonomics, source-rigor, freshness-monitoring |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | DR1 | surprise | (Developer review) **The bounded-live spend-breach class (runs 040 PDF, 047 JSON-extraction) was successfully avoided in 052 by naming the *class* ("variable-cost formats") in `fail_closed_when`, not just the prior instance.** Suggests run-047 DR1's decision to frame it as a class was load-bearing — the plan-writer hardened against the family and stayed at 3 credits of 8. | That the class-level rule is now permanently in place — it was written into this one plan; a future plan-writer who doesn't inherit it can still miss it. | developer-review.md; run-notes live_evidence_plan fail_closed_when; run-040 R1; run-047 DR1 | bounded-live-spend, tooling-ergonomics |
| 2026-06-25 | runs/052-2026-06-25-captured-price-freshness-decay | DR2 | gap | (Developer review) **A promo-expiry field fails the fillable-cut bar on a *new* ground: it is a rotting datum by design** — once the date passes, the field value is permanently stale and has no ongoing use to a reader. Distinct from engine-dev's "stays mostly-blank" failure mode: this field would fill reliably but become useless the moment it's consumed. First clear "would fill reliably, would rot immediately" instance. | That the field should never be built — the no-new-primitive landing is correct at n=1; only that the fillability argument alone doesn't close it; the "useful post-expiry" bar does. | developer-review.md Founder lens; read.md What Would Change; .claude/rules/engine-dev.md (fillable-cut bar) | freshness-monitoring, schema-edge-entity-type |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | S1 | surprise | **The graduated L001 coverage-radar recipe mechanically generalizes outside telehealth.** First non-telehealth test of the *graduated* L001 (all 3 source runs 012/022/024 were telehealth verticals): 1 SERP + 2 independent editorial listicles + a token-match store diff produced a clean, corroborated missing-set (Fitbit/Withings/Amazfit high-confidence; Garmin/Samsung/Google weaker) on consumer wearables. The mechanism is source-family-portable, not telehealth-specific. | That L001 is fully general — G1/G2 show its *assumptions* are telehealth-shaped; only that the recipe's mechanics produced a real answer on a fresh vertical. | read.md Result(2)/Verdict; receipt C1 table | query-time-grouping-enough, source-panel |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | G1 | gap | **Category blur splits the denominator into near-disjoint sub-axis listicles — a sharper `denominator-reconciliation` than telehealth.** The sleep-axis list (Oura, Eight Sleep, Bía, Muse, RISE) and the fitness-axis list (Hume, Fitbit, Huawei, Amazfit, Garmin) intersect on only Oura/Whoop/Withings (3 of ~15 distinct brands). In L001's telehealth runs "best GLP-1" lists overlapped, so "≥2 listicles corroborate" was robust; here *which two lists you pick drives the named-set*. L001 as written names no sub-category step. | That L001 is broken — it holds (S1); only that its "one category → one listicle population" assumption is telehealth-shaped and needs an explicit sub-axis step on fuzzy categories. | read.md Result(2)/C2 (corrected per VR1); receipt C1 (S2 vs S3) | denominator-reconciliation, source-panel, coverage-caveat |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | G2 | gap | **The store's own cohort boundary doesn't match the editorial category, so the diff denominator is contested on the *store* side before any matching.** The store's "wearable/recovery" set includes Peloton (connected fitness equipment), Therabody + Hyperice (recovery hardware), Nike (apparel + recovery partnership) — **none appears in either tracker list.** New flavor of `denominator-reconciliation`: contamination on the *store* side of the diff, vs the prior n=5 external-draw industry-cohort instances (036/037/039/042/045/046/050). | That the store tags are wrong — each is individually defensible; only that an entity-shape cohort ("trackers") can't be drawn from the store's broader "wearable/recovery" grouping without a human boundary call. | read.md Result(2)/C3; profile.md descriptions onepeloton/therabody/hyperice/nike | denominator-reconciliation, coverage-caveat |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | S2 | surprise | **The missing-set is the market's volume center; the store skews premium.** The high-confidence missing brands (Fitbit/Withings/Amazfit + weaker Garmin/Samsung/Google) are the mainstream $50–400 fitness-band/smartwatch tier; the captured 4 (Oura/Whoop/Eight Sleep/Apple) are the premium recovery/optimization tier. The store's wearable coverage is mode-skewed, not random. | That the store *should* capture the volume tier — "not captured" ≠ "not a competitor" (L005 corollary); only that the coverage gap is systematically on one market mode. | read.md Market Pattern; receipt C1 | coverage-caveat, source-panel |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | R1 | risk-miss | **Listicle rankings carry vendor/affiliate bias even where the named set corroborates (L004 instance).** Wareable ranks **Hume Band #1–2** while running "Buy 1 Hume Band, Get 1 free" + "50% OFF code WRBL20" + affiliate links (Hume is an advertiser on the page); the Circular SERP listicle ranks its own Circular Ring #1. The recovered *set* survives corroboration, but *rank and inclusion* are self-serving. Mirrors run-047 S2. | That the lists are useless — the corroborated set is reliable; only that vendor-authored/affiliate rankings are a biased denominator for *ranking*, not membership. | read.md C4; receipt C1 (S3 Hume advertiser, S1 Circular self-#1) | source-rigor, source-panel |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | R2 | surprise | **Bounded-live source-substitution worked cleanly when a planned list was unscrapeable.** PCMag (planned fitness-axis list) returned Firecrawl "we do not support this site"; substituting wareable.com (same family, in-plan) kept the run in-contract with no ceiling breach and no broadening. The plan's "≥2 independent editorial listicles" framing (family, not named URL) absorbed a dead source gracefully. | That source substitution is always safe — it stayed safe because the swap was same-family and in-ceiling; only that family-level (not URL-level) plans degrade gracefully when a source dies. See DR3 for the cost-class caveat. | run-notes live_evidence_used; receipt C1 (PCMag failed, not billed) | tooling-ergonomics, source-panel |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | VR1 | risk-miss | (Evidence verifier) read.md/run-notes G1 originally said the sleep + fitness lists "intersect on almost nothing but Oura/Whoop" — but **Withings appears on both full-scrape lists** (Sleep Foundation Sleep mat + Wareable ScanWatch Light), so the intersection is Oura/Whoop/**Withings**. A precision overreach; **corrected in read.md** to 3-of-~15. The steep-disjointness point survives. Continues the 042/045/047/048/050/052 verifier-catches-a-slip streak. | That the finding is wrong — disjointness holds; only that "almost nothing but Oura/Whoop" understated a 3rd full-source overlap. | read.md Result(2) pre/post; receipt C1 S2 vs S3 | source-rigor, denominator-reconciliation |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | VR2 | risk-miss | (Evidence verifier) read.md's flat "6 high-confidence missing" set conflated two evidence tiers: **Fitbit/Withings/Amazfit are explicit editorial *picks*** on a full list + SERP, while **Garmin/Samsung rest on a full-list *nav/related mention* + SERP snippet only** — thinner footing. The receipt Limits flagged it but read.md presented the six flat. **Corrected in read.md** to tier the set. Also: `apple-com` is a company-level capture, not an Apple-Watch-SKU capture. | That Garmin/Samsung aren't missing — they are (≥2-source); only that the read implied uniform confidence across a set with a real evidence gradient. | read.md Result(1) pre/post; receipt C1 Limits; S3 picks vs nav-mentions | source-rigor, coverage-caveat |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | CR1 | gap | (Consumer review) **The buyer-facing answer (the missing-set) is buried below the L001 framing.** read.md leads with the L001 generalization verdict; the buyer's "what am I missing" answer sits in Result(1)/Gap Map/Companies Seen below it. The buyer answer is present and good, but a buyer reading the top reaches the builder payload first. | That the read is mis-structured — the run contract names L001 generalizability as the primary question, so the framing is accurate; only that a coverage-radar with real buyer value would serve a buyer better leading with the missing-set. | consumer-review.md; read.md Result preamble vs Companies Seen | coverage-caveat, query-time-grouping-enough |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | CR2 | risk-miss | (Consumer review) **A coverage radar's binary present/absent diff silently inherits the captured set's quality variance.** Eight Sleep counts as "captured" (clock 2026-06-24) but its `offerings.md` was a mid-Prime-Day sale snapshot per run-046 G2 (`captured_at` carries no promotional flag). The diff treats it as equivalent to evergreen captures; doesn't affect the binary missing-set result, but the receipt notes no within-captured quality caveat. | That this changes the result — the captured/missing split is binary and Eight Sleep is unambiguously captured; only that a presence/absence radar inherits unflagged quality variance from the captured set. | consumer-review.md; receipt C1 S4 (eightsleep 2026-06-24); run-046 G2 | source-rigor, coverage-caveat, freshness-monitoring |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | CR3 | surprise | (Consumer review) **The most buyer-useful structural insight — the bimodal premium-recovery vs mainstream-band tier split — is the run's own Judgment, not a store-queryable ingredient.** No frontmatter field, cohort tag, or stored object encodes "tier"; a downstream system must re-derive it from the diff each time. "Map-not-ingredient" frontier (039/043 CR1) on a market-structure insight rather than an edge or price. | That the store should encode market tier — at n=4 captured it'd be a tiny rotting judgment; only that the run's most generalizable insight is synthesis output, not reusable State. | consumer-review.md; read.md Market Pattern; no store tier field | query-time-grouping-enough, coverage-caveat, denominator-reconciliation |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | DR1 | gap | (Developer review) **The `Market Pattern` bimodal framing was stated as a finding, not a Judgment, on a 2-list panel + 10-brand diff.** The store-level fact (capture skews premium, misses the fitness-band mainstream) is solid; the "bimodal market" inference extrapolates structure beyond what two sub-axis lists establish (the C2 2-list-artifact caveat applies here too). **Relabeled as Judgment in read.md** per this review. | That the bimodal framing is wrong — it's plausible; only that its confidence exceeded a 2-list panel and it wasn't labeled Judgment until corrected. | developer-review.md; read.md Market Pattern (corrected) vs C2 | source-rigor, coverage-caveat |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | DR2 | gap | (Developer review) **G1/G2 are L001/Recipe-9 scope-note candidates with no escalation path out of run narrative.** The graduated L001 (QUERYING Recipe 9) text implies "one category → one overlapping listicle population"; this run found that assumption breaks two ways on consumer hardware — but the caveat lives only in this run's read.md/run-notes. A future plan-writer reading Recipe 9 directly can't see G1/G2 unless a learning pass surfaces them. (Correctly NOT edited run-side.) | That L001 should be amended now or a lesson is due — out-of-band call; only that the generalization signal currently has no path to the lessons layer unless a pass reads this run. | developer-review.md; run-notes next-run advice; lessons.md L001 (no sub-category caveat) | coverage-caveat, tooling-ergonomics |
| 2026-06-25 | runs/053-2026-06-25-wearable-coverage-radar-l001-generalizability | DR3 | surprise | (Developer review) **Source-substitution safety is a joint function of source-family AND cost-class — not family alone (sharpens R2).** The PCMag→Wareable swap was safe because both are plain-markdown (~1 credit); the run-040/047 breaches were "same family, different cost class" (PDF/JSON-extraction, variable cost). The `live_evidence_plan` schema names neither cost-class nor enforces it as a substitution constraint. | That a schema change is needed — out-of-band; only that R2's "family-level plans degrade gracefully" holds only when the substitute shares the planned source's cost class. | developer-review.md; run-notes R2 + live_evidence_plan; run-047 R1/DR1; run-052 DR1 | bounded-live-spend, tooling-ergonomics |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | G1 | gap | No structured field encodes the buyer goal / use-case a company serves — grep for `use_case`/`jtbd`/`condition`/`vertical`/`indication`/`treats`/`audience` frontmatter returns zero across 145 profiles (verified independently). So the sleep/recovery substitute set is recoverable only by full-text reading of `description`+body — a reader Judgment, not queryable State. "Diagnosable but not queryable / map-not-ingredient" frontier (039/043 CR1) now at the **buyer-goal grain**. | That a JTBD field should be built — n=1 goal, open-ended buyer-framed cut; prose carries it for a human. "No new primitive needed" stays live. | read.md Result(1), C1; receipts/C1-no-jtbd-field.md | relation-pressure, query-time-grouping-enough, depth-backfill |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | G2 | gap | **5th sighting** that `primary_industry` ≠ entity/goal cohort key: 8 device/recovery members scatter across Technology / Healthcare & Life Sciences / Sports & Recreation (verified). **First demand-side draw** of the recurrence — prior four (036 G3, 037 G2, 039 DR1, 042 G3) were supply-side (group by what a company *is*); this groups by what a buyer *wants*. Spanning both sides hardens that the field is producer-shaped by construction. | That industry tags are wrong — each is individually defensible; only that the recurrence is now n=5 and spans supply- and demand-side cohort draws. | read.md Result(2), C2; receipts/C2-industry-scatter.md | denominator-reconciliation |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | S1 | surprise | The telehealth Rx-sleep entrants (rexmd/rugiet/malemd) are **doubly buried**: tag-disjoint from devices (`Services/Consulting`+`Biotech/Pharma Products`, not Hardware — verified) AND "sleep" is one minor line among ED/TRT/GLP-1/hair, visible only by grepping `description`. No device/wearable draw would ever surface them as sleep substitutes. | That they belong in the set definitively — they sell a real Rx sleep line; only that they're structurally invisible to any cohort draw. | read.md Result(4), C4; rexmd/rugiet/malemd descriptions | denominator-reconciliation, depth-backfill |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | G3 | gap | **Re-confirms** horizontal-substitute-relation absence (039 S1, 047): Oura/Whoop/Eight Sleep are mutual sleep-buyer substitutes but the store carries zero edges between them — relation support is vertical-only (`parent`/`owns`). The JTBD neighborhood can't be traversed even one hop. | That a substitute relation should be built — it'd be mostly empty/dangling today (039 W1 logic); only that the axis is structurally absent. | read.md Result(5); frontmatter (no horizontal relation field) | relation-pressure |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | S2 | surprise | The only shared structured tag, `offering_category:[Physical Products / Hardware]`, is simultaneously **over-inclusive** (19 profiles store-wide — verified to include watches/Casio/Apple/Ford/Warby Parker) and **under-inclusive** (misses all telehealth+supplement entrants). "Draw all Hardware" is both too wide and too narrow for the sleep/recovery JTBD. | That the tag is broken — it's a coarse leaf working as designed; only that it's not a JTBD key in either direction. | read.md Result(3), C3; 19-profile grep | denominator-reconciliation, query-time-grouping-enough |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | W1 | wish | Wanted a way to filter the store by **buyer goal** across cohorts. Lightest path if anything ever graduates is a **query-time recipe** ("grep description+body for the goal across all cohorts; ignore industry/category as keys") — NOT a JTBD field or substitute relation. Load-bearing reason: JTBD is open-ended/buyer-framed (fails the fillable-cut bar), no filter/sort consumer has appeared. Mirrors anti-sprawl W1 of 036/037/039/042/043. | That it should graduate now — only the lightest path *if* a filter/sort consumer + a 2nd cross-industry JTBD scatter appear. | read.md What Would Change; .claude/rules/engine-dev.md | query-time-grouping-enough, relation-pressure |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | S3 | surprise | Completeness is **method-bounded**: the substitute set was assembled by keyword grep (`sleep`/`recovery`/`melatonin`/`magnesium`/`circadian`); a brand serving the job without those tokens, or a whole solution type the store holds none of (CPAP/mattress/app), would be missed. Honestly flagged as "not found," not "not there" (L004 self-applied). | That the set is complete — it's explicitly partial; only that store-only completeness here is full-text-grep-bounded. | read.md Missing/Stale Coverage, C5 | coverage-caveat, source-rigor |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | CR1 | surprise | (Consumer review) Value lands on builder/Pantry not buyer for the 7th+ time, but with a **new sub-flavor**: here the per-member ingredient layer is genuinely decision-grade and the limit is **purely the cross-member index** (no JTBD shelf). Prior CR1s (038/039/041) had both a thin-ingredient AND a no-index problem; this run isolates the latter — the farm is good, the catalog lacks the shelf. | That this justifies a JTBD field — the "no new primitive needed" stay is in G1/W1; only that the builder-not-buyer streak has a sharper isolated-cause variant. | consumer-review.md; read.md Gap Map | query-time-grouping-enough, relation-pressure |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | CR2 | gap | (Consumer review) The assembled substitute set has a **soft center** whose inclusion is an unlabeled Judgment: Apple (catalog-grain, run-043 G2), Peloton/Nike (recovery fringe, neither self-brands as sleep). The run flags method-bounded *completeness* (S3) but not that **edge-member inclusion grain** (store-grounded vs reader-inferred) is invisible to a downstream reader. | That including them is wrong — they're plausible; only that the inclusion claim's grain (Judgment vs State) isn't surfaced. | consumer-review.md; read.md Companies Seen / Evidence Limits | query-time-grouping-enough, source-rigor |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | DR1 | gap | (Developer review) The proposed query-time recipe ("grep description+body for the buyer goal") has a **silent reliability gradient**: reliable for conventional-vocabulary goals (sleep/recovery), silently partial for jargon-fragmented goals or sparse older captures — producing a set that *looks* complete with no failure signal. The recipe framing (W1) doesn't yet name this gradient. | That the recipe is wrong here — it's appropriate for this goal; only that "grep for the goal" hides a vocabulary/vintage-dependent partiality risk. | developer-review.md; read.md C5 / Missing coverage; run-notes W1 | coverage-caveat, query-time-grouping-enough, depth-backfill |
| 2026-06-25 | runs/054-2026-06-25-sleep-recovery-jtbd-substitution | DR2 | surprise | (Developer review) The telehealth double-burial (S1) is a **structural inversion of L005's corollary**. L005 says "structured absence = coverage signal, not market fact." Here the entrants are **present and correctly tagged** — not absent but **misrouted**: a buyer-goal draw returns nothing because correct producer-shaped tags create invisible routing. "Tag-correct and therefore invisible" is a mechanism distinct from missing data (L004) and wrong tags. | That the tags are wrong — they're individually correct; only that correct producer tags can systematically exclude a captured player from a buyer-goal draw. | developer-review.md; read.md Result(4), C4; lessons.md L005 | denominator-reconciliation, schema-edge-entity-type |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | S1 | surprise | First whole-store (N=136) fill-rate census: the store's dependability is **tiered, not binary**. The required scalar contract is 100% filled, but "present" splits four ways — dependable-and-discriminating / subtractive-empty-but-meaningful / too-sparse-to-partition / telehealth-only-overlay. "Is the field filled?" alone does not tell a consumer what it can build on. | That any tier is broken — each works as designed; only that a consumer must combine fill-rate + value-concentration + entity_type-gating + the comment convention to know the tier. | read.md Result Tiers 1–4; C1 census | query-time-grouping-enough, coverage-caveat |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | S3 | surprise | A **100%-filled field is not a 100%-useful partition key**: `primary_industry` puts 52% of the store in one Healthcare pile, `entity_type` is 90% `Company`, `target_market` 43% `[B2C]`. High fill + high concentration = reads cleanly but groups lopsidedly. **6th `denominator-reconciliation` sighting** (036/037/039/042/054), the first *store-wide* and *concentration-flavored* one; whole-store version of run-039 G1. | That these fields are bad — dependable to read; only that fill-rate overstates their power as a *partition* key when one value dominates. | read.md Result Tier-1 + C2; run-039 G1 | denominator-reconciliation, query-time-grouping-enough |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | G1 | gap | The vertical-relation fields are **too sparse to be a population key**: `parent` 18/136 (13%), `owns` 16/136 (11%), `aliases` 80/136 (58%). Grouping on `parent`/`owns` silently drops ~87–89% of the store, and the field alone cannot separate a true-negative (`[]`, no parent exists) from a not-captured one. Vertical axis is structurally first-class (039 S1) yet populated too thinly to partition on; L005-corollary sized store-wide. | That a relation backfill or new field is needed — only that the existing axis answers "does THIS company name a parent?" but not "show me all subsidiaries." Spend/approval-gated if ever chased. | read.md Result Tier-3 + C5; run-039 S1; lessons.md L005 | relation-pressure, depth-backfill, coverage-caveat |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | S2 | surprise | Subtractive emptiness is **perfectly `entity_type`-gated**: every empty `business_model` (6), `portfolio_shape` (7), `offering_category` (3) belongs to an `Investor / Holding` entity. The closed set has no AUM/carry value, so the blank *encodes* shape rather than missing data. Whole-store confirmation of run-035's empty-business_model-for-investors sighting. | That the empties should be filled — they're correctly empty; only that a consumer must read them through `entity_type`, not as coverage holes. | read.md Result Tier-2 + C3; the 7 investor profiles | schema-edge-entity-type, query-time-grouping-enough |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | G2 | gap | The optional **module layers are a telehealth overlay, not store-wide substrate**: `offerings.md`/`visual.md`/`signals/` run 81%/49%/74% on the 71 Healthcare profiles vs 18%/11%/3% (Tech), 0/0/0 (Energy) — yet the profile *body* is uniform depth (~8–9k chars) across verticals. A downstream system consuming `offerings.md`/`signals/` as a store-wide ingredient is leaning on a telehealth-concentrated layer. | That the asymmetry is a defect — deep telehealth is an intentional cohort choice; only that the module layers are not a store-wide ingredient and their absence outside Healthcare is a coverage fact. | read.md Result Tier-4 + C6; module-presence-by-vertical table | coverage-caveat, depth-backfill |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | R1 | risk-miss | The **fail-loud-by-comment convention defeats naive structured parsing**: the store records subtractive emptiness as `business_model:    # empty — VC economics…`, so a parser that splits on `:` and takes the remainder reads the **comment as the value**. The operator's own first census pass counted `business_model` 100% non-empty until inline `#` comments were stripped, exposing 6 true empties. The human-protective guard is anti-machine-readable. Cousin of run-037 DR2 (unreliable second channel), on the structured-emptiness channel. | That the convention is wrong or the parser must change — only that a downstream consumer reading frontmatter values MUST strip inline comments first, or it ingests comment text as data. A read-side hazard, not a schema defect; no naive consumer is live today. | read.md Trap A + C7; first-pass vs comment-stripped counts | source-rigor, tooling-ergonomics |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | S4 | surprise | `unverified_fields` is the **one always-present guard field**: 136/136 non-empty — every profile self-discloses its soft spots. The single most reliable provenance/honesty surface in the store, and (unlike the headline fields) it never degrades to empty. | That it is structured or machine-graded — it's a free-text list; only that its *presence* is universal, the dependable hook for "what did this capture flag about itself." | read.md Result Tier-1 + C4; unverified_fields 136/136 | source-rigor, query-time-grouping-enough |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | CR1 | surprise | (Consumer review) The read's value lands squarely on the **Pantry/builder** consumer and is **nil for the end buyer** — but *by design*: a system-test/introspection read, not a market read. Continues the 038/039/041 CR1 "lands on builder not buyer" shape, here as an intended property rather than a shortfall. | That the read failed the buyer — it never targeted one; only that a whole-store reliability census is structurally a builder deliverable. | consumer-review.md Verdict; read.md Question/frame | query-time-grouping-enough, coverage-caveat |
| 2026-06-25 | runs/055-2026-06-25-field-fill-rate-queryability-census | DR1 | risk-miss | (Developer review / evidence-verify) read.md Tier-2 first phrased "7 investor empties" as if all 7 had an empty `business_model`; in fact 6 (the VCs) are empty and the 7th, blueowl, carries `business_model: Other` (closed-set misfit) and is empty only for `portfolio_shape`. Core claim ("every subtractive empty is `Investor / Holding`") survives and is *strengthened*. Precision slip caught + corrected in read.md; cousin of run-042 VR1. | That the finding was wrong — only that the count phrasing overstated `business_model` empties by one; the entity-gating story is intact and sharper. | developer-review.md; read.md Tier-2 (corrected); blueowl business_model: Other | source-rigor, schema-edge-entity-type |

<!--
Format example (copy a run-notes Observations row, prepend Date + Run):
| 2026-06-24 | runs/036-2026-06-24-some-slug | G1 | gap | Store can't see X at the needed grain. | A per-line dated note, not a brand field. | read.md C3; foo profile.md:22 | coverage-caveat |

Provenance: rows through run 035 live in ../_archive/discovery-ledger-legacy-2026-06-24.md
(the pre-migration ledger, old vocabulary). This table is the simplified stream going forward.
-->
