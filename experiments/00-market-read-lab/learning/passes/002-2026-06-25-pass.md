---
date: 2026-06-25
proposed: 5   # 4 new (L007–L010) + 1 advanced (L006)
left: ~146    # of ~171 new rows (runs 037–055)
---

**Pass summary.** Read all 181 active observations; the ~171 across runs 037–055 are the new corpus (pass 001 covered run 036). Zero `brian-correction` rows store-wide, so the Brian lane stays empty. The dominant shapes this pass were already *counting themselves* — several rows literally carry "3rd / 4th / 5th / 6th sighting" tags — which is the stream signalling that a few clusters have earned consolidation. I clustered into roughly nine shapes and proposed five (four new + one advance), leaving the large majority unconsolidated:

- **(A) industry/category draw ≠ entity-shape/goal cohort key** → **L007 (new).** The most-counted recurrence in the corpus (n≥6, supply- and demand-side).
- **(B) honest flags are content-complete but relay/salience-dependent** → **L008 (new).** Live risk against the #1 delegation value job.
- **(C) frontmatter inline-comment parse trap** → **L009 (new).** A sharp, grep-verifiable read-side `risk-miss`.
- **(D) bounded-live variable-cost-format class** → **L010 (new).** A *proven* fix (run-052 stayed in budget by naming the class).
- **(E) price-visibility token grain trap, fresh entity types** → **L006 (advanced).** The 2nd-entity-type precondition pass 001 set is now met 4×.
- **(F) decision-grade fact lives off the captured surface** → left; L003-strengthening sightings, not a new lesson.
- **(G) map-not-ingredient / value-lands-on-builder** (the CR1 chorus) → left; a probe-design frontier with a weak "replaces."
- **(H) freshness / no change-detection home / promo-bound decay** → left; F2 is the known traction-frame roadmap item, F3 (promo decay) is a real borderline I want a 2nd opinion on.
- **(I) single-valued `business_model` lossy on hybrids** → left; recurrent (n=3) with a specific parked fix, the other borderline I want a 2nd opinion on.

**Proposed / advanced.**
- **L007 (new · proposed)** — runs 037 G2 / 039 S2+DR1 / 042 G3 / 044 DR2 / 045 G1 / 054 G2 / 055 S3. Rule: a field's fill-rate and per-row correctness don't certify it as a cohort *partition key*; producer-shaped classifications (`primary_industry`, coarse `offering_category` leaves) scatter or contaminate entity-shape/goal cohorts, concentration weakens even 100%-filled fields, and `business_model` keys cleanly only when the cohort *is* its primary-tag semantics. Replaces: the industry/category cohort-draw reflex. Route: docs/recipe edit. The store-internal third member of the L004/L005 denominator family.
- **L008 (new · proposed)** — runs 038 R1+W1 / 042 R1+S1 / 049 G1+S2+W1 / 051 S3 / 055 S4. Rule: `unverified_fields` is universal (136/136) and honest, but protection is relay- and salience-dependent — a delegated agent, the brief's default 5-second view, or a structured-only read that drops/buries the flag launders self-reported facts into apparent truth; fix at the consuming surface, not with a new field. Replaces: "honest capture = safe delivery" + the reflex to add a provenance field. Route: docs/recipe edit. Surfaced under the severe-risk bypass (live risk vs the delegation value job).
- **L009 (new · proposed)** — runs 050 G2 / 055 R1. Rule: inline frontmatter `#` comments are ingested as values by a naive `:`/`[`/`,` splitter; strip them before keying. Replaces: the naive frontmatter splitter. Route: docs/recipe edit (carries an audit task — does any live recipe/`scripts/present` surface already key on `offering_category[0]` un-stripped?).
- **L010 (new · proposed)** — runs 047 R1+DR1 / 052 DR1 / 053 R2+DR3 (origin 040 R1). Rule: fail-closed on the *class* of post-fetch variable-cost Firecrawl formats (PDF, JSON-extraction), not per-instance; source-substitution must match cost-class, not just family. Replaces: per-instance ceiling hardening + family-only substitution. Route: docs/recipe edit (MRL run playbook). A proven fix.
- **L006 (advanced — still proposed)** — added 4 fresh-entity-type corroborations (037 S2/DR3, 044 S3, 045 S2, 046 S1) and the 037 DR3 scope-sharpening; the 2nd-entity-type graduation precondition pass 001 set is now met. No promotion — **ready for your accept/graduate call.**

**Brian-lane candidates.** None — zero `brian-correction` rows in the corpus.

**Deliberately left unconsolidated (~146 of ~171).** The honest majority. Specifically:
- **The "off-surface source panel" family (F)** — 036 G2, 037 Source Gaps, 038 G1+G2, 039 G2, 042 G4, 047 CR3, 048 G2+S1+DR1. These self-identify as "L003 analogue / Nth sighting of decision-grade-lives-off-site." They *strengthen* accepted L003 (and arguably argue for generalizing it from "review bodies" to a source-panel map) — but I did not mint a competing lesson or touch L003. Left as L003 sightings; a generalize-L003 call is yours.
- **The CR1 "map-not-ingredient / lands-on-builder-not-buyer" chorus (G)** — 038/039/041/047/048/049/050/055 CR1 (builder), 043/044/051/054 CR1 (toward buyer). A recurring probe-design frontier, but its anti-sprawl payload is already carried by L005 + the W1 chorus, and it names no crisp "replaces." Left; flagged below as a 2nd-opinion item.
- **The evidence-verifier streak** — 042/045/047/048/049/050/051/052/053/055 VR1/DR1 ("Nth consecutive run the adversarial verifier caught a precision slip"). This *validates* the Loop-2 harness; per your "validation earns no doc entry" rule it is deliberately **not** a lesson. Left.
- **Buyer-value positives** — 042 S2, 043 S1, 045 S1, 051 S1/CR1 ("store is a genuine strength for the reader"). Positives, not fix-demanding signals. Left.
- **Freshness F1/F2** — 041 G1+G2+S1+S2+W1, 047 CR2, 048 G1+S3 (captured_at semantics; no append-only change-detection home; level≠delta). F2 is the known traction-frame roadmap item — confirmed, not new. Left.
- **All W1/anti-sprawl "no new primitive needed" rows** across 037–054 — the stream's self-gating chorus; left intact as divergence.

**The two I most want a second opinion on:**
1. **Promo-bound price decay (F3)** — 046 G2 / 052 G1+S1+S2+DR2 / 043 S3. Crisp and distinct: a captured promo price's staleness is bounded by its own *printed expiry window*, not `captured_at`; the predictive datum is in prose; and 052 DR2 contributes a genuinely *new* engine-dev argument — a promo-expiry field would *fill reliably but rot immediately on consume* (distinct from the "stays mostly-blank" failure mode). I held it to keep the pass disciplined, but it could earn a lesson now (route: docs/recipe edit + a fillable-cut-bar addendum).
2. **Single-valued `business_model` lossy on hybrids (I)** — 037 S1+R1+W1 / 044 G2+W1 / 051 S2. n=3 entity-sets, with a *specific* parked fix (ranked multi-select reusing `offering_category`'s precedent, gated on a filtering consumer). It could be a no-op anti-sprawl lesson now (like L005) or stay watched. Your call on whether the specific-and-recurrent fix earns recording.

**Anti-Merge attestation.** No observation was edited, merged, summarized, deleted, reordered, or stamped this pass. No accepted lesson (L001–L005) was edited. The only writes are L007–L010 (`proposed`) appended to `lessons.md`, an advance to L006's source-observations + notes (kept `proposed`), and this pass note. Compression happened only by adding lessons that point at rows.
