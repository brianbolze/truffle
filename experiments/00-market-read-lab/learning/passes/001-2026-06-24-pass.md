---
date: 2026-06-24
proposed: 1
left: 7
---

**Pass summary.** Read all 9 active observations (run 036, marketplace-schema-fit — the first non-telehealth probe), plus L001–L005 and the empty `brian.md` as dedupe context. No prior passes. The whole active corpus is one run on one vertical, so the governing discipline this pass is *don't overfit one vertical into general rules.* Clustered into four shapes: **(A)** a price-visibility token grain-trap on two-sided entities [G1+R1]; **(B)** marketplace economics as an uncaptured off-site ingredient [O2+G2+W1]; **(C)** "structured-absence ≠ market-absence" / "schema-can't ≠ firm-didn't-on-site," plus the positive cohort-cut sightings [S1, O1, G3]; **(D)** a headline scale figure that misleads without its scope caveat [R2]. Only shape A earned a new proposal; B/C/D are confirming sightings of already-accepted lessons and stay unconsolidated.

**Proposed / advanced.**
- **L006 (new · proposed)** — from run 036 G1 + R1. Rule: the price-visibility token certifies buyer-reachability of the *consumer* price only; on two-sided / intermediary entities it is silent on the entity's own take rate, so a `[published]` token over-reads as transparency. Replaces: reading the token as a completeness signal on non-DTC / two-sided entities (a reading convention, not a field). Route: docs/recipe edit. Surfaced despite n=1 because it is a `risk-miss` against a live SCHEMA token and a reader's decision surface (the severe-risk bypass).

**Advance candidates (no edit made — await your nod to fold these source pointers into the accepted lessons).** Run 036 supplied independent confirming sightings; I did not touch the L001–L005 rows this pass:
- **L005** ← O1 (`business_model` names the marketplace cohort positively via a designed-for closed set), G3 (`business_model` recovers the cohort that an industry-draw is blind to), S1 ("not captured ≠ not true," economics flavor).
- **L003** ← O2 + G2 (marketplace take-rate / GMV economics is a decision-grade ingredient living in an uncaptured off-site source family — filings / IR — the marketplace analogue of review bodies).
- **L004** ← G3 (a naive industry-draw denominator is partial / selection-biased for entity-shape cohorts).
- **L002** ← R2 (a headline scale figure — Etsy's consolidated GMS — reads decision-grade only when its house-of-brands scope caveat travels with it; extends L002's family from Signals into State scale figures).

**Brian-lane candidates.** None — no `brian-correction` rows this pass.

**Deliberately left unconsolidated (7 of 9).** O1, O2, S1, G2, G3, W1, R2. All are single-run, single-vertical sightings. The economics cluster (O2/G2/W1) is self-gated by W1's own anti-sprawl ("lightest prose path only *if* a real cross-marketplace consumer + a 2nd homogeneous cohort appear") and is already covered by the union of L003 + L005 — minting a marketplace-economics lesson now would be overfitting one run. O1/G3/S1/R2 confirm existing lessons rather than open new ones. The one I most want a second opinion on: **L006** — the only proposal, resting on n=1 run (two review lanes). The open question is whether the severe-risk-miss bypass justifies proposing now, or whether it should wait for a 2nd entity-type sighting beyond marketplaces.

**Anti-Merge attestation.** No observation was edited, merged, summarized, deleted, reordered, or stamped this pass. No accepted lesson (L001–L005) was edited. The only additions are L006 (`proposed`) in `lessons.md` and this note; compression happened only by adding a lesson that points at rows.
